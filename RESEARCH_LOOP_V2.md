# 研究循环 V2

保存日期：2026-08-10

## 目的

这套流程把策略翻译、研究判断和代码编写分开。研究优化必须依据完整的开发期结果，不能根据少量总指标猜原因，也不能读取最终测试时期的结果。

## 执行顺序

```text
实验 YAML
  -> 策略翻译 Agent
  -> 研究优化 Agent 先做训练期研究
       -> compare_core_variants / diagnose_only：固定程序比较，不增加策略试验次数
       -> test_candidate / branch_hypothesis：批准一个正式候选
       -> request_data：记录缺少的资料和恢复条件
  -> 代码撰写 Agent
  -> 固定程序检查
  -> 开发期回测
  -> 固定程序验收
       -> 通过：冻结候选，等待明确允许后做一次最终测试
       -> 未通过：研究优化 Agent
            -> test_candidate：回到代码撰写 Agent
            -> branch_hypothesis：明确另开相关假设后回到代码撰写 Agent
            -> compare_core_variants：固定程序比较三至五个事先写好的简单版本
            -> diagnose_only：固定程序补算，不增加策略试验次数
                 -> 补算结果返回同一轮研究优化 Agent
            -> request_data：结束当前运行并写明取得什么资料后继续
            -> stop：结束研究
```

## 各角色职责

### 策略翻译 Agent

- 提示词：`src/quanta_agents/prompts/agents/strategy_translation_agent.yaml`
- 只把 YAML 翻译成明确、完整的策略说明。
- 不写代码，不看回测，不优化参数，不增加交易规则。
- 必须写清核心假设、固定部分、可改部分、候选研究方向、决策时刻、执行时刻、公式、参数、数据和无法实现的要求。
- 结果字段可以用于训练期汇总评价和规则选择，但不能用于单个事件的实时筛选、排序或仓位。

### 代码撰写 Agent

- 提示词：`src/quanta_agents/prompts/agents/strategy_agent.yaml`
- 只把已经批准的策略或单项修改写成完整 Python。
- 不解释亏损原因，不选择下一项研究，不自行增加参数或规则。
- 固定程序报错时，只修复明确指出的代码问题。

### 固定程序检查和开发期回测

- 不调用大模型，因此没有系统提示词。
- 检查 Python 是否能运行、数据列是否合法、日期含义是否正确、目标比例是否合法，以及是否读取禁止字段。
- 对 `fixed_target_until_exit` 日线计划，代码生成后先检查原目标是否保持到原退出日；历史指数成分只限制新建或增加目标。决定日由统计验证检查，下一实际执行日由本地回测检查；已经成功开始的计划不会因持有期内退出指数而提前清仓。
- 使用开发期分段、双倍费用和延迟一个执行时点等结果进行评价。

### 研究优化 Agent

- 提示词：`src/quanta_agents/prompts/agents/research_optimizer_agent.yaml`
- 读取完整开发期报告和全部开发期候选记录。
- 至少提出两个可能原因，分别列出支持证据、反对证据和缺少的证据。
- 必须比较上一轮预计变化和实际变化。
- 每轮最多批准一个主要变化，并写明什么结果说明当前判断错误。
- 每轮至少提出两个不同方向，并列出尚未正式测试的方案。
- 分别判断价格现象、费用可行性和实际可交易性，不能互相替代。
- 证据不足时选择 `diagnose_only`，不得猜参数。
- 核心版本需要比较时选择 `compare_core_variants`；相反机制需要另行研究时选择 `branch_hypothesis`。
- 单字段筛选只能提名候选，不能代替正式回测。
- 固定补算不算正式候选；通常至少完成两个正式候选后才允许停止。
- 禁止读取或推测最终测试时期结果。

### 固定诊断程序

- 不调用大模型。
- 接受固定诊断编号，日期只能从实验设置读取。
- 当前分钟事件支持分年分月、逐步筛选、单变量特征筛选、5/10/20分钟收益、下一批放量与上涨拆分、核心规则组合、事件数量核对、20分钟限制前后比较、收益与费用分布等项目。
- 单变量特征只能用训练期选择条件，2024只检验相同条件；结果字段不得用于筛选。
- 初次研究只读取训练期：较早部分用于选择，较晚部分用于内部检查，不读取2024。
- 核心规则组合最多五组，每组最多五个条件，只允许使用决策时已经知道的字段。
- `diagnose_only` 完成后返回同一个研究轮，不增加 `epoch_index` 或策略试验次数。
- 每个研究轮最多补算两次，避免重复请求同一项目。

### 固定程序验收

- 由 `ManagerAgent` 按实验 YAML 中预先规定的要求判断，不调用大模型。
- 验收结果不能改写回测数字，也不能自行新增标准。
- 最终测试时期结果不能返回研究优化 Agent。

## 保留的研究记录

每个开发期候选保存：

- 完整分段、双倍费用和延迟测试结果；
- 本轮事实、可能原因及正反证据；
- 本轮预计变化和判断错误的条件；
- 上一轮预计结果与实际结果的比较；
- 已测得事实、已经检验的结论和仍未验证的猜测。

分钟事件研究还必须保存：

- 原始候选、无效收益、非零信号、成功匹配、冷却限制删除和最终事件数量；
- 毛收益均值与中位数、毛上涨率、净收益均值与中位数、净胜率；
- 结果标签名称、有效标签数、标签为真的数量、标签比例及其区间；
- 按年和按月结果；
- 0 倍、1 倍和 2 倍费用下的事件均值。

毛上涨率、净胜率和结果标签比例是三个不同指标，不能互相替代。

## 停止研究的条件

- 必要资料确定无法取得且没有替代，或策略在允许的数据和市场规则下无法计算；或者
- 核心简单版本、有证据支持的候选和合理的相关假设都已完成正式回测，连续正式候选没有稳定改善。

只有一个正式候选时，除非存在前述硬性问题，否则不能停止。费用未覆盖、T+1限制、补算次数用完或单字段候选失败，都不能单独作为停止整个因子研究的理由。

停止前必须列出尚未测试的方案、不测试原因，以及停止范围是当前版本、真实交易写法还是整个研究。

## 分钟量能实验运行方式

只检查资料：

```powershell
.\scripts\run_periodic_active_buying_loop.ps1 -Profile minute -CheckOnly
```

运行三轮开发期研究，不读取最终测试时期：

```powershell
.\scripts\run_periodic_active_buying_loop.ps1 -Profile minute -MaxEpochs 3
```

从已有实验的研究诊断位置恢复：

```powershell
.\.venv\Scripts\python.exe scripts\resume_research_loop.py `
  --source-experiment <旧实验编号> `
  --source-yaml experiments\periodic_volume_minute.yaml `
  --new-experiment <恢复后的新实验编号> `
  --max-epochs 3
```

恢复结果写入新实验目录，旧实验记录不改动；恢复时会隐藏最终时期并关闭最终测试。

只有规则已经冻结且得到明确允许后，才增加 `-RunFinalTest`。

## 当前限制

- 分钟事件文件没有真实订单、持仓、可成交数量、停牌和涨跌停状态。
- 没有符合A股T+1的离场规则前，5分钟结果只能用于研究价格现象，不能解释为真实账户收益。
- 恢复入口根据运行记录重建研究状态；普通启动仍会从第一轮开始。
