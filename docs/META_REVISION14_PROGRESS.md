# v14 公共研究工具与监控验收

2026-09-07。**未达标。** v14 通过了公共题库工作台和监控的组合工程验收，尚未接通模型研究循环，没有新增正式策略、架构比较或前瞻结果。

普通题包现在可以通过公开合同调用输入查看、三期限诊断、条件匹配对照，并提交唯一终稿。工作台持久保存每轮规格、机会预算、子项、失败、完整产物和本侧公开历史；未知动作阻止新轮，恢复只核验保存记录，不重新计算。16 次查询、12 个候选、1 次终稿的限制适用于每个工作台，不能冒称是账户总费用上限。所有统计明确为毛事件端点，尚无组合仓位、交易成本或净收益评分。

独立审查发现超长有限 JSON 被拒后没有消耗机会，可免费更正。原两条失败反例、旧源码和报告完整保留。修复后流式计算原请求哈希与字节数，保存有界拒绝记录并扣除相应机会；超长终稿也结束工作台。公开合同升级为 v2，字段限制与整体 65,536 canonical UTF-8 字节上限共同适用。见 [修复附录](research/meta_framework_v14_handoff/casebank_workbench_repair_001.md)和[独立审查](research/meta_framework_v14_handoff/casebank_workbench_independent_001.md)。

组合验收由两份不同范围的已完成验证组成，不重复计数：

- 工作台修复后的作者 25 项及独立 8 项，共 33 项通过；根代理复核收据和当前源码、测试哈希，未再次运行该组。
- 新监控改动的 13 项定向检查通过，另有 7 项乱序异步响应检查通过。新增面板显示开发原价覆盖、题库/工具状态和工程用量快照，与研究费用分开。

v13 的 228 个继承文件仍与复制清单相同；v14 的继承文件仅监控模块变化，其余 227 个未改，新增工作台、测试和验收脚本另列。旧两次付费诊断、原四题包、盘点联合产物和单事件来源探测均未改变。冻结 v14 源码 SHA256：`64f72b6057ceb815cf706d05e52de2b276e659583aa41b68ccb84fa3dd8408e2`。见 [组合验收收据](../experiment_traces/meta_ashare_revision14/validation_attempts/20260906T195723544594Z/receipt.json)。

只读监控沿用 `http://127.0.0.1:8776/`。核对本任务旧 v13 服务的进程、命令与端口后完成替换，保留原启动和退役记录；未建立第二个持续监控。实际浏览器确认 553,230 已知研究 token、零未结预留、28/28 完整调用、原失败与复校合法终稿分开，以及历史成员范围 559,770 行中 11,708 行缺失/异常。页面显示保存时点，不冒充工作进程或供应商模型认证。

执行链专项只读审查确认现有原股数账本可复用，优先缺口是保存行情与因果目标的接线、逐步保存的失败资金路径，以及实际触及范围的公司行动、状态、限价、费用与容量合同。平安银行不在本次 846 股名单内，单事件探测不能充当该股票池公司行动覆盖。明确保守模拟假设可以用于工程研究，但不能重标为真实执行认证。见 [执行桥接缺口](research/meta_framework_v14_handoff/execution_bridge_gap_001.md)。

**这一步做得怎么样？** 已把只有名称的公共工具变成可调用、可恢复、可审计的工作台；独立反例推翻并修复了一处预算边界。工程通过的限度明确：四个实际题包只做加载和输入查看，未跑候选发现；未获独立同预算正控资格，也未形成真实收益成功率。

**下一步如何改进，什么会推翻判断？** 已创建隔离 v15，继续实现受控多轮研究循环及原股数持久桥接，先以离线假网关和公开算术数据检验。研究循环必须绑定模型调用、动作、前状态、阶段共享预算和唯一终稿，保持完整本侧上下文，未知回执不重派。原股数路径必须在持仓缺价或权益未知时保留此前资金事件并停止，不能删除损失路径。如果出现重复派发、跨侧轨迹泄漏、子尝试漏计、决策晚于结果、恢复重新回测或把保守假设写成已认证，应撤销相关通过结论。付费研究仍关闭，旧两题不再复校，原 846 股不再续读。
