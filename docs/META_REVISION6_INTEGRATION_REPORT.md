# 第六版：同研究机会的控制器与执行诊断

状态：2026-09-06 工程验证通过，第六版独立监控已启动，Q 计划已冻结、正在启动前核对。第六版位于 `experiment_traces/meta_ashare_revision6`，主源码与第三、四、五版完整文件集合与内容核对不变。当前不能据此认定策略目标达成。

## 本次改动解决什么

旧八项实验的候选将几乎全部机会用于组合回测，基线分出四次机会做诊断。旧结果不能区分指导的研究能力增量与工具机会差异。本版以共同控制器固定六个时隙：诊断、回测、诊断、回测、回测、提交。最后只能提交本次同一架构已经成功评估的策略，或诚实报告没有受支持的策略。诊断无法识别时允许弃权，但消耗该时隙，不能换取额外回测。失败、重复和弃权继续留在完整分母中。

执行诊断是双方共同环境。每次原有回测附带逐日资金与成本桥接、连续持仓段、加仓/部分卖出以及期末尚未闭合的持仓信息，来自实际模拟成交档案。模型收到汇总与固定数量的时间顺序样例，完整表另存。净损益桥接通过也不把当前复权单位模拟升级为真实股份执行认证。

## 恢复与证据身份

独立检查发现了两处正常恢复可触发的问题，修前证据全部保留：

1. 执行 ZIP 丢失后，用当前时间重建会改变压缩档案哈希。新档案固定成员顺序、时间与文件元数据，恢复时必须匹配旧登记哈希；不匹配则保留原登记和明确拒绝记录。
2. 诊断报告的文件哈希正确，仍可能属于另一策略或数据。恢复现核对请求策略、观察、数据、源码和日期范围，以及模型可见的完整汇总；缺失报告只能对原冻结策略做确定性重放，并要求完整观察与报告一致。恢复事件明确记录没有增加模型调用或搜索机会。

已完成但进程异常退出的模型回答，仅在最终文件、流式输出、完成事件、模式约束及用量全部可核对时采用已有回答。意外出现后续新轮时，用量保持不完整并进入预算预留，不能继承上一轮的完整用量。旧失败批次不回写成成功，也不进行付费补跑。

## 验证记录

- 旧八项的 91 份执行档案已经由诊断模块离线复算，最大会计残差约 3.49246e-9 元。该次绑定诊断 v1 源码；后续 v2 只收紧模型可见样例数量，不能把旧验证身份改写为最终联合验证。
- 诊断 v2 的诊断、Case 接入和大小边界共 52 项定向测试通过。独立 Decimal 场景同时包含亏损、盈利、重新建仓、跨年和未闭合持仓，经济恒等式通过。
- 恢复修正有 7 项独立生成账本检查通过，包含原 ZIP 身份保留、重建拒绝和报告语义关联。第二位审查者的独立 12 场景矩阵也达到预期，原先 6 类语义误收全部被拒绝；正确报告零重算复用，缺失报告仅原策略重放。
- 第一项真实数据零模型流程 `meta-20260906-210948-66d78c` 因内存分配错误失败；已完成初始证据、一次诊断和一次回测，在第二次诊断时退出。3 条固定响应、0 模型 token，原失败保留，不替换为新尝试。
- 额外合成矩阵检查连续 12 轮因子计算，私有内存约 990–997 MB，峰值约 1,121 MB，没有复现持续积累。这不排除真实流程中的其他内存问题。
- 第二项真实数据流程 `meta-20260906-213210-c6ce03` 完整通过，242.907 秒、六个固定步骤、四份诊断档案、0 模型/0 token。连续采样显示私有内存峰值 2,186.578 MB，系统最小可用物理内存约 14,438.715 MB。没有复现首项错误，首项根因仍未确定，不能宣称已修复内存问题。
- 最终联合回归 **659 passed，91.64 秒**（外层记录 93.797 秒），无跳过、失败或错误；源码与测试身份均保持不变。另 **12 项 Node GUI 检查通过**，包括弃权和已检验策略提交状态。
- 真实浏览器检查确认固定日程、六条两问自检、零模型标签及完成后控制禁用。完整诊断预览呈现 6,240,314 字符并能关闭；首次读取在大型 JSON 排版时达到浏览器 3 秒上限，后续截图与 DOM 检查正常。弃权视觉文字由 Node 用例验证，不冒充已跑过真实浏览器弃权场景。

最终包源码为 `46608b307615ab87422ad95e6835b7ff882d813a7f3dd550c50a0b2713e81228`，77 文件；验证凭证 SHA256 `7ec66f3f80cedf13e5ec32618f4c866b6ad4bc09ed3a0d218c74cd032ab282c7`。凭证记录验证时尚未部署，后续部署状态单独保存。

## 独立运行台与历史来源

新运行台为 <http://127.0.0.1:8771/>，新账本目录 `experiment_traces/meta_ashare_v6`。旧 8769 服务保持原样：自动审批拒绝了停止旧 GUI 并切换进程的操作，仅返回 `blocked by policy`；没有改用其他方式关闭它。

新目录用 SQLite backup 取得一致性历史副本，并逐一核对 1,152 个复制文件的 SHA。9 个原运行、104 个原调用、1,814 个原事件和1个留出访问记录逐行相同。导入的是同一批历史实验，绝非新增独立重复或新费用；原绝对来源引用保留，旧检查点未导入。导入凭证为 `meta_ashare_v6/lineage_import.json`。

新批次为 `q_20260906_01`，计划 SHA256 `9b38166fbef5bb8a2aede7f521cdf256c401d7539849371cde855d836d04aed6`。冻结时四项均未启动，用量为0；后续状态以该目录的 `report.json` 为准。

2026-09-06 22:10 香港时间，Q按冻结计划启动。22:36通过现有控制暂停最后一项；22:47复核批次仍为paused，3项failed、1项paused，四次实际模型调用共81,279已知token，没有付费补跑。独立只读审计确认三个失败调用都有最终完成事件、相符的完整回答和用量，涉及61,758token；网关把已恢复的重连错误粘为终态失败。原失败未回写，缺失的退出码不推测为0。现成接口不能安全导入这些未登记回执的回答，需隔离修订和独立恢复协议。见 `experiment_traces/meta_q_gateway_audit/20260906T143902468130Z/audit.json`。该真实缺陷未被先前工程测试覆盖，Q尚不能给出架构优劣结论。

评测解释补充：旧批次回测次数差异可属于相同总预算下的架构资源分配效果，不能一概称整个比较无效；它阻止将增益专门归因于诊断推理。Q固定日程检验条件机制效果，不能替代自由分配预算的整体效果，也不能直接与旧批次作单一改动因果对照。

## 下一步研究边界

Q 预定两个普通题目、每题两种指导，共四次独立运行，顺序 AB/BA；全部实际模型请求仍锁定 Astra/xhigh。每次六调用、600,000 token 事故上限，整批 2,400,000 token 与 16 小时事故上限。此次只使用 2017–2021 开发期，2024–2025 最终期继续封存。

Q 估计相同研究机会与共同工具环境下，原候选指导是否仍有条件增量。四次运行不支持稳定成功率或唯一机制归因；尚未实现的机制承诺循环 M 将另行冻结评测。正式净 Sharpe > 1 仍需要完整原价股份执行、公司行动和容量证据，以及独立跨题、跨时期的冻结验证。

## 两问自检

**这一步做得怎么样？** 工具机会、提交身份、弃权和恢复已有可执行约束，独立检查发现并修复了真实证据恢复缺口。659 项联合回归、真实数据流程及浏览器检查通过，但保留首项未解释的内存失败。新版已独立部署，历史来源未被算作新增实验；工程成绩不替代策略研究成绩。

**下一步该做什么，如何改进？** 启动前独立核对冻结 Q 计划，随后按固定顺序运行和监控，保留全部失败与用量。根据轨迹、有效诊断和配对结果决定研究机制的下一项改动，不用中途分数回改任何一方的指导或工具。
