# 元框架最小闭环验收记录

验收日期：2026-09-05。本次为真实模型调用加合成行情联调，不是收益研究结论。

## 真实闭环

- 运行 ID：`meta-20260905-225149-9f70b8`
- 最终状态：`completed`；8 个流程步骤完成。
- 模型请求全部固定：`gpt-6-astra / xhigh`；没有使用低价模型或低 effort 替代。
- 正常闭环共 5 次调用，各步骤 attempt 均为 1；约 703.20 秒活动时间（11 分 43 秒），暂停时间不计入。
- 提供方报告输入 86,001 token，其中缓存输入 6,272；输出 10,770 token，其中推理输出 8,803；总计 **96,771 token**。
- 缓存与推理分别是输入、输出的子集，不重复加总。五次成功调用用量均已返回；未推测金额。
- 实际执行的 Codex 原生程序报告版本 `0.153.3`。当前系统命令别名另指向 `0.144.1`；网关保存每次实际可执行路径和完整参数，不能混用别名版本作为执行版本。
- Python 3.10.11，LangGraph 1.2.10，langgraph-checkpoint-sqlite 3.1.1。
- CLI 事件未独立报告模型身份与 effort 两项，所有回执 `model_verified=false`，明确记录请求锁定及验证限制；未通过询问模型身份制造确认。

| 步骤 | 报告 token（输入＋输出） | 调用耗时 |
|---|---:|---:|
| 基线初始研究 | 17,626 | 75.11 秒 |
| 基线诊断改进 | 17,867 | 119.72 秒 |
| 元架构修改提案 | 19,141 | 140.02 秒 |
| 候选初始研究 | 20,836 | 264.95 秒 |
| 候选诊断改进 | 21,301 | 101.42 秒 |

元研究员提出的修改名为“实验前核对判别证据”。它从基线轨迹识别出部分解释需要的对照证据没有在复核时齐备，建议先检查问题是否能由可得证据回答，再花费实验预算。候选指导及其父版本、修改假设、风险均已保存。

合成 final 区间上，基线最终策略 Sharpe 约 0.2088，候选约 -0.8085；本次候选没有优于基线。系统仍完整保存其结果，`promotion=false`，没有因为提案解释合理就宣布架构改进。这个结果只描述单次合成案例，不能据此判定架构普遍优劣。

## 故障、恢复与 GUI 验收

真实运行在第一步模型完成后暂停。当时账本为 1 次调用、17,626 token。随后终止并重新启动本地 GUI 服务，通过页面恢复，直接进入 `baseline_refine`；第一步没有再次调用，最后五个步骤全部只有一个 attempt。

浏览器实际验证了页面启动真实运行、状态更新、公开推理摘要、事件分类筛选、token 细项、暂停与恢复、产物 JSON 预览。页面控制台没有发现错误。停止、人工消息污染标记、未知用量预留、明确重试及跨来源请求拒绝由定向测试验证；没有为测试停止按钮额外中断成功闭环。

最终 52 项定向测试通过，覆盖：

- 次日成交、现金占用、双边费用、跳空止损及未来数据扰动隔离。
- 非法 DSL 拒绝、固定模型配置、真实事件解析、未知 usage、取消与超时清理。
- 暂停恢复、失败结果明确重试、无效已保存结果不永久复用、预算与用量不重复加总。
- 数据库事件游标、产物路径限制、本地来源与 Host 检查、重复 worker 锁。

联调中另保留三个未完成运行：`99fdfa`、`9c4da5`、`8b076e`。前两次在加载 CLI 配置时被拒绝，分别涉及内置 provider 配置覆盖及不存在的 `tools.view_image` 字段；第三次把启动诊断错误识别为工具执行，现已修正并增加测试。失败尝试没有被删除，其未返回的模型用量保持未知；上面的 96,771 token 只属于成功闭环，不代表整个账户或所有联调尝试的总消耗。

真实运行后仅补强恢复回执校验、跨运行超时设置及终态墙钟计时；52 项测试包含这些补强。原运行冻结源码快照保留原版本，没有改写历史产物。最终版本另以离线流程复验，不需要重复支付同一模型实验。

## 交付入口

- 启动脚本：`scripts/start_meta_gui.ps1`
- 本地 GUI：`http://127.0.0.1:8767`
- 架构与运行文档：`docs/META_FRAMEWORK.md`
- 原始证据：`experiment_traces/meta/meta-20260905-225149-9f70b8/`
- 关键产物：`candidate_architecture.json`、`frozen_submissions.json`、`final_evaluation.json`、`comparison.json`、`calls.json` 及每次调用的原始交互 ZIP。

MVP元修改范围仅为研究指导文本，策略采用固定受限DSL；尚未接入V2自由代码研究、真实行情竞赛、跨家族盲测或自动架构晋升。
