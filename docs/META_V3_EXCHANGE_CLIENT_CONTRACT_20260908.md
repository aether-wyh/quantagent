# 上交所公告查询的客户端参数核对

本阶段找到了此前手工请求缺少的三个分页字段。按页面代码补齐后，仅发出的一次修正查询仍返回与原请求逐字节相同的115字节错误正文，没有公告列表。服务端错误原因仍不明；本查询分支已停止。

## 已保存的证据

封闭范围007读取页面明确引用的`common_table.js`：1次GET、8,324字节。该脚本处理表单，没有目标分页函数。范围008依次读取3份明确加载的脚本：`sse_full.js`为菜单数据，`main_2021.js`声明并先加载`search_common_2021.js`，后者包含实际分页及请求函数。前两次未定位目标函数的读取照实保留。

原公告模块第653行以参数1调用列表方法，第1129行调用`paginationChange`，第1155行随后调用`getJSON`。公共模块第1266—1296行的`dynaGetData`对非零页参数返回该值；第1400行开始的分页函数把它写入`pageHelp.pageNo`、`pageHelp.beginPage`、`pageHelp.endPage`。原请求已有pageSize=25和cacheSize=1，缺少上述三个值为1的字段。

修正决策在请求前保存，绑定六段原始代码的文件hash与行号。修正仅添加这三个字段，保留原600037、2018-02-06至2018-02-08日期窗口、接口、回调和其他过滤条件。配置的请求头与原采集器一致。旧请求意图没有直接保存headers；旧头部证据来自封存采集器第74行，新意图则直接记录了headers。两次均无线上报文抓取，不能称为独立证实所有实际发送头部相同。

修正响应HTTP200，正文中的success是字符串`false`，errorType为`ExceptionInterceptor`，没有result字段。HTTP成功不等于公告查询成功，这也不是有效的空公告列表。字段遗漏已确认；该遗漏导致原错误的因果解释未被证明，缓存等服务端因素也未查明。没有证据支持权限挑战或全站不可用的判断。

## 范围、成本与封存

007与008合计5次GET、454,619字节；其中4份脚本、1次修正索引查询。008本身为4次GET、446,295字节，严格用完3份脚本加1次修正请求的上限。未自动重试、跟随重定向、更换请求头/传输方式/接口、扩展日期或继续其他股票查询。PDF取得不在008授权范围内，本阶段没有下载PDF。

25项离线证据检查通过。它们检查已保存文件、请求时间和hash连接、字段差异、响应及旧账；不是25个工程测试。首次封存脚本误以为旧意图含headers，出现KeyError，在写入review或closed前停止；原脚本及控制器错误记录已保留。修正为静态读取封存采集器的请求头参数后第二次封存通过，没有再次访问网络。控制器错误记录不是独立原始终端日志。

007清单8个文件，008清单25个文件，各清单不计自身。旧006的18个文件、041的164个文件及041全部17个提交项均未改动。008逐次请求绑定时间戳和此前URL清单；这不追补006旧补充清单未独立记录的分派前时间证据。当前2026页面代码也不构成2018公告历史完整性的证明。

8份供应商账本实时只读核验不变：57次调用、1,427,801已知tokens、V3未知预留0。平台控制器累计用量另计。本阶段新增实际模型、账户执行、native Job、工程测试、真实或封存行情读取、生产源码变更均为0。

## 与研究目标的关系

041已由父任务独立复核为`reviewed_no_new_findings`。该阶段只有一份完整生成账户附件保存了完整输出；部分账户、旧路径与纯检查结果的证据是冻结测试源码和JUnit，未另存全部内存结果。未重复未改的专门测试集合不表示`validate`或`_value`未执行，它们仍是比较函数依赖。

本阶段新增原始公告0、金融样本0，未解决2018税龄日期、送转来源、完整覆盖或真实成交等证据缺口。正式成功分母仍为0，full_stack仍未准入，每次实际调用的运行时模型身份仍未确认。工程复核与查询诊断均不能作为净Sharpe或完整目标达成的证据。

007、008均已关闭，同分支不再改写关键词、请求头或额度继续尝试。主控下一步应转向有明确准入依据的实质研究轨迹，验证模型如何选择少数可推翻预测并回应矛盾证据；不得把重述旧2019结果或再次调参包装为新增独立样本。该后续尚未执行。
