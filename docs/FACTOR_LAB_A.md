# A 层因子实验室（A11）操作说明

建立日期：2026-09-13。设计依据：`docs/research/framework_review_20260913/FRAMEWORK_REVIEW.zh-CN.md`。与 V10A 并行，互不影响；V10A 的 F 盘研究进程保持运行。

## 与旧框架的差别

| 项目 | V10A | A11 |
|---|---|---|
| 股票池 | 沪深300 历史成分 | 全 A 可交易（非 ST、非退市、上市 ≥120 日、有成交额）；沪深300/500/1000 作为诊断池 |
| 指标 | 原始值 Pearson，逐年硬门槛 | 日度截面 RankIC 年均值 + ICIR + 最差年；Pearson（缩尾 z 分）作诊断 |
| 字段 | 开高低收量额 | 增加 vwap、流通/总股本、流通/总市值、换手率、市场收益 |
| 预处理 | 无 | 排名/缩尾/标准化；市值中性化作为显式算子 |
| 组合 | Ridge | LightGBM / Ridge / 等权，逐年扩展窗重训 |
| 模型参与 | 一次 20KB 提案 + 400KB 记账复盘 | 文件式请求；子代理可用训练期探针工具，多轮短调用；复盘问研究问题 |
| 探索空间 | 窗口 × 算子 | 机制 × 数据来源 × 变换 × 条件；程序维护机制覆盖图 |
| 对照臂 | 无 | Alpha158 简化集 + LightGBM 锚定；遗传规划搜索 |

验收数字不变：单因子六年均值 RankIC > 0.05（最差年 > 0.05 为强通过），组合 > 0.10。2019—2024 为已曝光历史，只能称"六年历史达标"。2025 数值不加载。

## 命令

所有命令在仓库根目录执行，`PYTHONPATH=src`，Python 为 `.venv\Scripts\python.exe`，模块 `quanta_agents.factor_lab_a.cli`，研究根 `F:\A_Layer_Research`。

| 命令 | 作用 |
|---|---|
| `build-panel` | 从冻结 parquet 构建宽面板（2015—2024），写入 `panel/` |
| `init` | 写入冻结协议 `protocol.json` 与初始状态 |
| `run-set --set classic/alpha158/legacy` | 登记并评价参考集 |
| `evaluate` | 评价所有未评价候选；自动做冗余检查与入池 |
| `eval-expr --expr` | 单表达式完整评价（含 2019—2024） |
| `probe --expr` | 只返回 2016—2018 训练期统计；给研究子代理用 |
| `gp` | 遗传规划搜索（训练期适应度），胜者登记为候选 |
| `combine --model lgbm ridge equal --target raw rank --update expanding fixed rolling3y` | 池内组合逐年重训 |
| `set-anchor --file` | 把某次组合结果登记为锚定基线 |
| `propose-request` / `ingest-proposal` | 生成提案请求目录 / 读入子代理响应并评价 |
| `review-request` / `ingest-review` | 生成复盘请求 / 读入复盘 |
| `status` / `report` | 状态 JSON / 中文报告 |

## 研究循环

1. 参考集（classic、alpha158、legacy）与遗传规划先入池，跑 `combine` 得到锚定基线。
2. `propose-request` 生成批次请求目录；由 Opus 5 high 子代理读取 `prompt.md`、按需运行 `probe`、写 `response.json`。
3. `ingest-proposal` 登记并完整评价；`combine` 更新组合；`review-request` 交子代理复盘；`ingest-review` 保存决策。
4. 下一批提案自动带上覆盖图、池、失败清单与上一次复盘。

入池规则：方向由训练期决定且 |t| ≥ 2；六年均值 RankIC ≥ 0.015；与池内任一成员的采样日排名相关 < 0.9；池上限 80。低边际 IC 但低相关的因子允许入池供组合使用。

## 独立重放验收

`python -m quanta_agents.factor_lab_a.replay --root F:\A_Layer_Research --file batches/<组合结果文件> --index 0 --seed 7`
从冻结面板按表达式重新计算全部成员、重新用训练期确定方向、按同一更新规则重新拟合并重算六年 RankIC，结果写入 `acceptance/`。与保存记录的差异应只来自模型随机种子。

## 2026-09-13 首轮结果（已曝光历史，非样本外）

| 项目 | 结果 |
|---|---|
| 参考集 | 经典 22、Alpha158 简化 81、旧库 46；遗传规划两个种子各 20 |
| 单因子 | 六年均值 RankIC > 0.05 的候选 72 个（含数值重复），最差年 > 0.05 的 42 个；最高为遗传规划因子 0.094 / 0.076，经典因子中特质波动 0.085 / 0.070 |
| 锚定基线 | Alpha158 简化 50 特征 + LightGBM：原始目标 0.075 / 0.059；排名目标 0.1005 / 0.086 |
| 池组合 | 96 成员 LightGBM 排名目标逐年扩展重训（隔日采样）：**0.112 / 0.0999**；109 成员逐日采样：**0.1134 / 0.10003**（2019—2024 逐年 0.118/0.124/0.100/0.119/0.108/0.111）；三年滚动更新更差（0.110/0.096）；加入第三批后 116 成员 0.1127/0.0984 |
| 模型层实验 | 同一 109 成员加市值上下文 + 三种子平均（隔日采样）0.1131 / 0.0991，对照 0.1121 / 0.0999，无改善；按均值取前 100 成员（含遗传规划种子 3）0.1123 / 0.0941，2021 更差——按单因子均值选成员会向波动/换手簇集中 |
| 独立重放 | 96 成员组合换种子重算：0.1121 / 0.0998；109 成员逐日组合换种子重算：0.1134 / 0.0994。均值稳健，最差年（2021）在 0.10 ± 0.001 随种子波动 |
| 模型提案 | 三批共 41 个候选，来自 Opus 5 子代理，含三个目录外新机制；第一批最佳 0.063、第二批最佳 0.046；主要贡献是正交成员与复盘诊断（发现组合数据集因单个低覆盖成员丢掉 73% 样本） |

三轮复盘的关键结论：训练期 t 值是 √N 放大的统计量，不能当效应量；"2021 是长窗口年"的假设被第三批证伪（250 日候选 2021 年相对自身均值平均 −0.010）；池内 120 个因子只张成约 8.5 个独立方向，日频量价信息集接近天花板；组合层杠杆（目标改排名、模型改梯度提升各约 +0.02）比成员数量（约 +0.001）大一个数量级。

结论边界：达标口径是全 A 池 RankIC，且 2019—2024 已在选择过程中曝光；独立样本外稳定性与交易盈利未证明。

## A11.1：海量枚举与批量改造（2026-09-13 起）

用户方向：模型擅长诊断、不擅长生成，因此因子生成改为"程序海量枚举 + 模型剪枝诊断"。新增：

| 命令 | 作用 |
|---|---|
| `harvest --sets alpha101 alpha191 calendar` | 把本地外部因子库作为插件候选登记并评价：Alpha101（101）、Alpha191（191）、因子日历日线库（标准 89、风险 40、附加 25）。插件因子不经表达式语言，直接由原库函数在面板上计算；缺行业数据的 INDNEUTRALIZE 用截面去均值近似并标记 |
| `expand --seeds pool/source:X/ids:... --top N` | 对种子因子批量改造：窗口邻域（表达式种子）、市值/换手/波动中性化、指数平滑、时序标准化、符号对数压缩、截面排名、剥离 20 日反转、5 日变化、60 日时序排名；只在训练期筛选（最差年 IC、t 值、覆盖率、与池及已筛变体的数值新颖性），存活者登记后走完整评价 |
| `backfill-ranks` | 为池成员生成 float16 的逐日百分位排名数组，组合器改用内存映射读取，不再整体载入成员矩阵 |
| `combine --members diverse:0.7` | 按训练期 t 值贪心选成员，成员间采样日排名相关 < 0.7 |

组合结果新增可交易性指标：预测前十分位五日超额、底部十分位超额、多空价差、前十分位日换手、逐年前十分位超额。

### A11.1 首轮结果（2026-09-13 16:00）

| 项目 | 结果 |
|---|---|
| 候选规模 | 875 个候选（外部库 446、改造变体 129、遗传规划 110、模型提案 41、参考集 149），已评 822，池内 355 |
| 各来源六年均值 RankIC > 0.05 的数量 | 遗传规划 52/60、改造变体 57/129、Alpha191 24/188、因子日历 22/124、Alpha158 简化 14/81、经典 8/22、旧库 10/45、模型提案 5/41、Alpha101 2/82 |
| 改造效果 | 129 个通过训练期筛选的变体里 31 个六年均值高于母因子、28 个最差年更高；最有效的改造是 5 日指数平滑与窗口翻倍（最高 +0.023） |
| 组合 | 多样性筛选 150 成员 0.1132 / 0.0974；全池 355 成员（每四日采样）**0.1159 / 0.0978**，前十分位五日超额 +0.39%，前十分位日换手 41% |

判断：因子数量从 135 增至 355，组合均值只提高约 0.0025，2021 年仍在 0.098 左右；单因子最强者仍是遗传规划（0.094），外部库里最好的是因子日历的市值调整换手率（0.085）和 Alpha191 第 159 号（0.083）。数量策略把单因子层填满了，但组合层的天花板由信息集决定，不由数量决定。

## A12：纯因子收益评价、交叉复合与换手控制

| 命令 | 作用 |
|---|---|
| `combine ... --save-prediction` | 落盘逐日逐股分数，并报告多头前十分位（逐日/五日分批）、多空的毛/净超额、夏普、换手、回撤 |
| `portfolio --prediction <文件> [--cost 0.002] [--buyable] [--smooth 5] [--holds 1 5 10]` | 对已落盘分数做纯因子组合评价：成本率、剔除次日开盘涨停、分数指数平滑、持有期 |
| `portfolio`（无参数） | 扫描池内全部成员的多头收益并写入结果（进入准入与父代评分） |
| `crossover --parents 40 --pairs 60` | 程序按质量/新颖度/探索不足/深度选父代，跨簇配对，七种复合算子生成子代，训练期筛选后评价；父代、算子、增量写入谱系账本 |

准入新增多头路线：多头前十分位五日分批净夏普 ≥ 1 可入池。协议记录了组合目标：多头前十分位五日分批、单边 0.1%、基准同池等权，净夏普 > 1 且净超额 > 15%。

## A13：可信度、分钟信息源、交易层（2026-09-14 起）

新增模块与命令（仓库根目录，`PYTHONPATH=src`）：

| 命令 | 作用 |
|---|---|
| `python -m quanta_agents.factor_lab_a.credibility recompute --root F:/A_Layer_Research --min-t 2 --workers 3` | 为所有 \|训练期 t\| ≥ 门槛、尚无排名矩阵的非池候选生成排名矩阵（写入 `cand_ranks/`，与 `pool_ranks/` 同格式） |
| `credibility select --min-t 4 --max-corr 0.9 --sources clean\|all` | 只用 2016—2018 的口径重选成员：\|训练期 t\|、训练期股票覆盖率 ≥ 0.8、按 \|t\| 降序贪心、训练期采样日签名相关 < 门槛；`clean` 只含生成过程不看曝光期的来源（经典、Alpha158、旧库、Alpha101/191、因子日历、遗传规划） |
| `credibility combine --members-file <members json> [--top N] [--label-blend 5 10 20] [--smooth-lambda 1.0]` | 对任意成员清单重训 LightGBM 排名目标逐年扩展组合（默认低内存路径），报告 IC 与多头前十分位（逐日/五日/十日分批、剔涨停、成本 0.1%/0.2%）；可选多周期标签融合与换手惩罚目标 |
| `credibility attribute --research-pred <pred> --oos-pred <pred> --out <json>` | 多头端归因：等权基准与四个风格（市值、换手、波动、动量）多空收益、前十分位风格倾斜与市值五分位构成、分市值五分位的多头超额与 IC、十分位收益、多头超额对风格收益的回归、逐月拆解 |
| `credibility directions --members-file <json> [--rows train\|all]` | 成员签名相关矩阵的谱：参与比、特征值 > 1 的个数、解释 80/90/95% 方差所需分量数（"有效独立方向数"） |
| `python -m quanta_agents.factor_lab_a.minute_features build --out F:/A_Layer_Research/minute_daily --workers 6` | 从分钟库聚合 31 个日内日频字段（`im_` 前缀：时段收益与成交占比、已实现方差与上下行比、偏度峰度、大单代理、量价相关、量能分布形状、零收益占比、日内 Amihud、路径效率、收盘对 vwap 偏离、日内回撤、高低点时序） |
| `minute_features assemble --daily <dir> --panel-dir <panel>` | 把日内字段按面板日期×代码写成宽表 parquet，随后表达式语言、枚举、改造、交叉引擎直接可用这些字段名 |
| `cli run-set --set minute` | 登记分钟基础集：每个日内字段 × 10 种时间聚合（当日、5/20/60 日均值、10 日指数平滑、20 日标准差、20 日时序标准化、市值/换手中性化、5—20 日差）共 310 个候选并评价 |
| `cli gp --minute` | 遗传规划终端集加入日内字段 |
| `python -m quanta_agents.factor_lab_a.trading grid --prediction <pred> [--cost 0.002]` | 交易层网格：分批持有（1/5/10/20 日）与缓冲带（进入前 5%/10%、退出带 0/5/10/20%、最短持有 1/5 日）× 分数平滑，按净夏普排序 |
| `trading train-long` | 每个候选在训练期（2016—2018）的多头前十分位十日分批净超额与净夏普（用于不看曝光期的"按成本后多头超额选成员"） |
| `trading freeze --prediction <pred> --rule <json> --out <dir>` / `trading replay --panel-dir <panel> --scores <dir>/scores.parquet --rule <dir>/rule.json` | 落盘冻结版本的逐日逐股分数（可买池内百分位）与交易规则；独立重放只依赖分数、规则和开盘价 |

组合器低内存路径（`combine.rolling_combination_lowmem`）：一次遍历成员排名文件写入半精度行主序内存映射（训练采样行 + 测试行），LightGBM 经 Sequence 接口分箱一次，各目标年用 `Dataset.subset` 取"年份 < Y"的行前缀训练，测试分块预测。396 成员复现 A12：0.1166 / 0.0977，十日分批 0.2% 净超额 +16.1% / 1.92，逐年训练行数与原路径完全一致。

本机约束：提交内存可用约 10GB（其他应用占 25GB），重算 ≤ 3 进程、分钟聚合 ≤ 6 进程，不并行两批；样本外面板的 vwap 已改为前复权口径（此前为原始价，80 个用 vwap 的成员的样本外数字失真）。

### A13 结果摘要（2026-09-14，修正后路径）

| 项目 | 结果 |
|---|---|
| 选择膨胀（曝光 396 对训练期口径 309，三种子） | IC 0.0011—0.0031；十日分批 0.2% 净超额 1.2—1.8 个百分点（全在 2024 年）；协议口径（五日 0.1%）为零；只是准入通道的下界 |
| 样本外（面板修复后） | 2025：IC 0.136、十日 0.2% 净 +14.4%、五日 0.1% +16.5%；2026 年 1—5 月：IC 0.082、十日 0.2% 净 −7.3%（毛 −4.8%）、市值中性后 +1.6% |
| 2026 归因 | 风格暴露解释 −10.9 个百分点（研究期 β 外推），剩余 α +6.1%；前十分位 71% 在最小市值五分位；4—5 月顶部排序失灵 |
| 分钟信息源 | 31 个日内字段（第 2 版）；池 396 → 597；独立方向参与比 12.8 → 15.4；曝光池组合 IC 0.1166 → 0.1183、十日 0.2% 净 +14.9% → +16.7%（六年全升）；训练期臂 IC 反降 0.002、多头净超额 +0.7—1.1；2025 样本外未复现（低 1.6—2.8），2026 略好 |
| 训练目标 | 5/10/20 日标签融合：IC 0.1192 / 最差年 0.1015、十日 0.2% 净 +18.1%、换手 6.0%；换手惩罚只对逐日调仓有用 |
| 交易层 | 缓冲带（进前 10%、跌出前 30% 卖、5 日平滑）比十日分批净超额 +3.3、净夏普 +0.37；市值中性回撤减半 |
| 冻结版本 v13 | 研究根 `frozen/` 下四个目录（597 成员，原标签 / 多周期标签 × 缓冲带 / 市值中性），各含分数宽表与长表、可买池掩码、规则文件、样本外子目录与重放结果。主线 = 多周期标签 + 缓冲带：六年净 +22.3% / 2.82、回撤 −16.8%，2025 +22.9%，2026 −1.7%；市值中性版 +16.2% / 2.62、回撤 −5.8%，2026 −3.1%；原标签缓冲带 +20.0% / 2.62，2025 +19.5%，2026 −4.8%。独立重放命令见上表 |
