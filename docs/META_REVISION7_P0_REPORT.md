# Revision 7：P0恢复、预算、批次控制与GUI验收

日期：2026-09-06至09-07，香港时间。主控任务：`01a0774d-4327-7701-948f-2796de6fccd3`。正式工程评审配置：gpt-6-astra / xhigh。

**P0离线工程验收通过；A股策略目标仍未达标。** 下一隔离修订 revision8 已进入两道诊断校准题和公共证据工具实现，尚未启动付费研究筛查、架构比较或最终留出。

## 证据与最终版本

- 冻结源码：`experiment_traces/meta_ashare_revision7/src/quanta_agents`，SHA256 `fc6543a181d7c1fc71967b44272cd6a85cfceafef43ed5eeb576ed49925c429b`。
- 联合检查：`experiment_traces/meta_ashare_revision7/validation_attempts/20260906T155410352749Z/receipt.json`，**776 passed**，164.05秒测试、166.625秒完整核验；源码与测试在运行期间稳定，主源码和第六版与接手时哈希相同。
- JavaScript界面检查：**14 passed**。真实浏览器另核对8774最终源码的初始待启动、批次暂停、刷新后暂停持久化、解除后仍待启动且8未启动/0活动/0token、重新暂停、4,887,667字节旧诊断副本的64KiB分页和关闭预览。真实研究启动按钮禁用，服务也在API层禁止付费运行。
- 实际恢复：`experiment_traces/meta_ashare_v7_recovery/report_20260906T153509729223Z.json`，3份已保存回答分别登记、核验和一次性导入；重复执行返回原导入，原Q仍3 failed / 1 paused。

## 修复行为

网关区分可恢复的中间重连错误和真实失败终态。所有退出路径核对最终完成事件、保存回答与事件内容、schema、最终输入/输出用量以及可获取的请求/轮次身份。重复完成、多轮冲突、真正失败、截断、不一致、无最终用量均有拒绝反例。新调用单独写进程退出证据；旧记录缺失就保持未知，没有补造exit=0。

历史Q缺少新版供应商请求绑定。恢复用冻结外部审计、原账本prompt hash、原schema、文件字节哈希和事件本地身份进行兼容核验，标记 `local_artifact_bound_unverified_provider_request`。这不是供应商幂等性或供应商模型身份认证。三份回答涉及**61,758个历史token**，只作原调用的引用，不再次加入已付费总数。Q全部4调用已知token仍为81,279。

`completion_recovery.py` 与公开命令 `scripts/reconcile_saved_answers.py` 将意图、历史预留引用、核验结果及导入记在独立恢复账本，原Q运行、调用、事件和文件不回写。导入类型为 `saved_answer_import_only`，没有执行其中的研究动作、没有补最终提交、没有新研究启动。后续若使用这些回答，必须另建有来源的派生继续研究，不能称独立样本。

运行账本在调用前用SQLite事务登记意图和预算预留；并发连接共享检查与提交。未知/失败或缺最终用量的调用保留预留，不能以已知部分当完整账单，也不能自动重试。预留和派发阈值是事故保护，不是供应商账单绝对上限。

批次有独立持久控制意图、派发锁和连续3次同类基础设施异常断路器。暂停会封闭新派发，成员失败不能擦除批次暂停。暂停HTTP确认未知时仍显示成员边界未确认，避免盲发重复POST。解除暂停只开派发闸：未启动/成员间隙显示ready，暂停成员保留paused并提示单独恢复。接口不冒充调度进程存活证明。

GUI读取当前控制状态；旧无控制文件的批次继续展示有界历史报告，不尝试读其旧计划。跨端口复制计划不能被当前控制路由转发。大型JSON以64KiB字节页显示原文片段，完整下载保留；分页不是完整JSON对象解析。

## 失败和修正记录

所有阶段失败原样保留，不以最后通过覆盖：

1. 新离线联合运行器第一次将Popen替换为函数，触发asyncio类继承错误，未收集测试。保存启动失败记录后改为子类拦截真实研究调用。
2. 第一次774项联合检查772过：旧无控制批次的invalid plan兼容失败，以及一次Windows本机连接中断。仅修复报告读取条件；48项server定向检查通过。未对本机中断臆测确定根因，最终联合检查也未再复现；若重现应继续调查。
3. 浏览器发现未派发夹具解除暂停错误显示running；新增两项状态反例，22项控制组通过后重跑最终776项。
4. 历史prompt的Windows换行与原运行字符串hash不同，第一次离线恢复拒绝保留；改用与原runtime一致的逻辑换行hash，原文件字节哈希不变。
5. 独立导入审查发现旧目录防写时机、已存application再核验以及整数/小数语义相等边界。修复后12项导入独立矩阵包含在最终776项中。

## 控制权和运行台

原主任务接手时已idle；现有每小时监控ID `a` 已迁至当前主控，未创建第二个监控。`docs/META_CONTROLLER_CHECKPOINT.json` 是交接入口；主控正在工作时监控只读，状态不变安静。

- 8771：旧第六版Q服务，保留暂停，无重复派发。
- 8774：最终P0离线验证GUI，`experiment_traces/meta_ashare_v7_gui_verified`，只运行夹具。
- 8773：修复前离线验证GUI，保留供追溯，不作为最终源码验收或研究调度器。
- 8769：旧服务保留，不绕过此前停止拒绝。

自动审批再次拒绝了停止并重启8773的操作，原因仅为 `blocked by policy`。该操作没有执行；使用独立8774实例完成了验收，没有改用其他方法停止被拒绝的进程。

## 两问自检与下一阶段

**这一步做得怎么样？** 已以原事件而非付费重试复现和修复P0错误，联合测试、独立导入审查及真实浏览器发现补齐了具体漏项。新增研究网关调用为0，不能据此把工程agent/主控实施评审的模型成本报成0；这部分尚无完整供应商账单，应单列未精确核对。供应商请求身份、绝对费用上限、真实运行暂停确认丢失及调度活性仍有明确边界。导入回答不是新研究结果。

**下一步该做什么，如何改进，什么结果会推翻判断？** revision8先做两道已暴露开发诊断原型：原始订单/独立日历可查询，事件期限共享完整样本，独立Decimal/时序oracle与同权限参考客户端，查询/最终报告预算和答案隔离反例。只有两条离线路径通过才冻结实际Astra/xhigh校准预算。任一重复付费、暂停后越界派发、未知费用释放、篡改旧记录或GUI误报活动会推翻相应P0结论；任一公共接口不可达或oracle泄漏会阻断校准。

P1真实收益执行链、隐藏正负控、P3自主循环、P4公平比较与P5前瞻验证仍未完成。原价公司行动、历史状态与容量等门继续为未合格；不读取当前封存2024–2025市场，不改变净Sharpe>1及跨案例/重复/前瞻标准。
