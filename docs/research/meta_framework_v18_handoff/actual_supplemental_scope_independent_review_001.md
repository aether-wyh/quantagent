# v18 唯一实际补充窗口准入独立复核 001

**结论：准予主控基于本次精确 scope 另行创建明确的新阶段准入、唯一登记，然后在原账域按正常控制器流程继续原四题。** 本报告及审计程序本身没有创建 admission、登记阶段、执行模型或工作台动作，也没有写入实际数据库。此结论不是原六小时阶段成功或正式策略成功。

- scope：`experiment_traces/meta_casebank_supplemental_preparations/reference_v15_original4_001_v18s1_scope_001/scope.json`，SHA256 `8d60a4fdef3d94c4f952edb721f9d620c62a17f51c760e2b67a4e9ed9beef66b`。
- 独立回执：`experiment_traces/meta_casebank_supplemental_scope_reviews/reference_v15_original4_001_v18s1_001/receipt.json`，SHA256 **059620e3e117dd09c6c961704c34d752f754bd7ce6d6d9cdf25c4ad1d3f31c7d**。同目录保留审计程序、读取清单与诊断记录。
- 唯一新增绝对窗口：**2026-09-07 04:16:47.524738–10:16:47.524738 UTC**，21600 秒。核验时尚余约 20830 秒；等待消耗此窗口，不能重新计时。原截止 **03:01:23.682649 UTC** 与 `expired_incomplete` 保留。

15 项来源 pins、工程接受回执 `58ed0c0c…195b75`、107 项完整源清单 `a4465429…68885f`、7 个运行时文件及 CLI `429e0d7d…c44da9` 均逐字核对。最终 phase 为 `83e04e88…5aa392`，core 为 `6e38e602…4f9919`。准备和登记脚本的当前 SHA 与另一独立静态修复审查一致；本次不重新运行工程测试或 GUI 检查。

完整五库保留回执 `9dd35d09…8cabec` 的来源路径、任务对应、备份名和 SHA 全部匹配；五个当前数据库的原始字节及全部表、列值、行序均与冻结快照相符。使用 SQLite `mode=ro`，authorizer 仅允许 SELECT/READ/FUNCTION。审计结束再次核对五个源库字节未变，当前不存在补充阶段表或登记。

当前事实为：原 v15 run/call、v16 correction、v17 原 scope/admission 和四个历史事件原样保留，原 admission 仍 `paid=false`；治理 paused。两笔累计传输包含 **11235 已知 token + 80000 原未知预留 = 91235 名义暴露**，没有新的未知、pending 或 v17 模型调用。唯一已应用动作仍是原 correction 的 `inspect_inputs`，与第一题原 WB 的已完成 slot、请求、回执和 observation 一致；保存观察 SHA 为 `ab6bc701…58408`。第一题查询数 1、候选/终稿 0；后面三题全部未开始，顺序及 baseline 臂未变。核验没有读取题目 CSV、私有 evaluator、原价格源或密钥。

原上限仍为每题 17 / 全阶段 68 传输，每题 16 查询 / 12 候选 / 1 终稿，60 万 / 240 万名义 token，单次预留 80000；原时钟和全部已消费次数均不重置。原四题各自历史和来源身份保持绑定；继续使用已保存的第一题查询，不重新执行该动作。任何新 unknown 停止补充阶段；第三窗口及自动延期不获授权。窗后本地收尾仅限本新窗口内明确准入且派发、随后完整保存并绑定本阶段的回答，不能用旧 correction 冒用豁免。

主控的新 admission 应绑定本独立回执、精确 scope/source/CLI、完整五库保留回执及 runtime observation；登记前仍须重核这些固定材料及当时状态。若期间发生记录变化、来源不符、pending、预算不足或窗口已关闭，应按原门停止，不能用本报告覆盖变化。后续结果只记为补充开发结果：原描述任务分母仍是 4，新增独立启动与正式策略分母增量均为 0。请求模型与实际供应商身份需分列，当前供应商身份仍未验证。

审计程序曾两次停止在事件哈希表示的核对：先误用解析后的 body，继而误带 SQLite seq。冻结协议实际绑定 `identity + 原始 body`；修正审计脚本后通过，完整数据库行比较始终另行保留 seq。两版程序与诊断原样保存，没有修改 scope、产品或实际行，也没有把审计脚本停止伪称产品反例。

两问自检：**是否新增免费机会或翻转旧失败？** 没有，新增的是明示有限时间，旧费用、未知、原截止和未完成状态保留。**是否由本评审实际派发或认定研究成功？** 没有；本轮构造控制器/Store/Gateway、实际写入和执行均为 0，工程评审模型用量未单独测量，研究能力和正式成功尚无结论。
