# 原计时阶段到期后的研究路径：独立设计审查 001

2026-09-07，Astra/xhigh，仅设计；没有修改源码、执行 scope、原库或调用模型/工作台。本次只读原预注册、预算、范围及 v17 工程源码/说明，没有读取私有机制、实际价格或题目数据。03:01:44 UTC 的工具时钟已越过原 **03:01:23.682650 UTC** 下一调用准入截止；本文不声称已代主控完成阶段关闭。

**建议关闭原计时阶段为到期且不完整，再另冻一次有限的补充开发时间窗口，继续原四题及完整本题历史。现在换新未暴露题的信息价值较低。** 新窗口确实增加时间资源，属于明确的政策修订，不能称“原预算完全没有增加”，不能把后来 final 回填为原六小时内完成，更不能以新 registry、新 task 或新 seed 重启分母。

依据是：主控给出的当前研究证据仅两次传输、一个保存的 `inspect_inputs` 动作、没有 final；第一笔确定 schema 拒绝，第二笔回答可验证。原材料的大部分调查尚未进行，现有中断还不能判断研究者是否能形成有证据的结论。继续同题能直接观察“公开输入→自选候选→证据核对→final/弃权”，而换题会同时改变输入、上下文和工程条件，也放弃已经付费的动作。这里使用主控当前账务摘要，未重新打开 actual 账本；本地动作是否已经应用必须由具体冻结快照决定，不能猜成 0 或 1。

## 不可混淆的两份结果

| 对象 | 必须保留的判定 |
|---|---|
| 原计时阶段 | 在原截止关闭新模型准入。主控冻结原状态、全部费用、失败、未开始任务及无 final 事实；如到期时仍无 final，原阶段就是合法 final 0/4，不能拿中间回答补位。基础设施失败与研究行为错误分开归因，但不删除原阶段失败。 |
| 一次补充开发阶段 | 原四个 task、同一公开包/工作台、固定顺序、同一 arm 和原 rubric。最终报告如产生，只能记为补充阶段产物；独立任务增量 0，原四题分母不变。可评价累计调查质量，不能与严格六小时运行宣称等时公平，也不能归为新的独立 baseline 重复。 |

研究者每轮仍为 tools-disabled 的新调用上下文，但内容必须延续本题全部真实已核验模型回答与已实际公开的 observation，包括纠错回答及其唯一应用结果。无回答的 HTTP 拒绝保留在审计账，不伪造一段失败回答；工具失败依原公开合同保留。不能混入其他题、私有机制、评审推断或新的人工解题建议。仅增加一般资源告知：“原窗口已结束，现为一次补充开发窗口；累计剩余传输/动作/额度如下”。如已发生答案或特征提示泄露，同题只能记人工辅助开发，不能继续声称自主普通输入调查。

## 一次补充窗口的最小约束

建议窗口上限仍取 **21,600 秒，最多一次**，供主控在实际准入前选择更短值；这不是本文件创建的期限或派发许可。全部工程及本地保存动作审查完成后，另冻绝对 `start/end`，到时才可准入；冻结后准备等待、暂停、账户中断都消耗该窗口，不顺延。原创建时间/截止永久保留；分别显示两个窗口和从原创建开始的总经过时间，工程/评审用量另列。再次到期或基础设施故障仍未形成 final，就保留不完整结果，不自动滚动第三个窗口。正常已准入推理沿用单次 3,600 秒收尾规则，不为了时钟切短正常推理。

金钱和机会仍用**一个原逻辑总账**，不是另给一套配额：

- 原已知 input/output 11,235 与旧 billing-unknown 80,000 均保留，当前名义 exposure 为 91,235；不声称这是供应商最终账单。新预留 80,000 后是 171,235。只有指定的旧确定拒绝债务允许作为已批准的 quarantine 携带，任何新 unknown 都停全阶段。
- 首题已用 2/17，最多剩 15 次传输；全域已用 2/68，最多剩 66。每题 600,000、全域 2,400,000 名义 token 准入值、单次预留 80,000 均不增加。当前首题名义余额 508,765、全域 2,308,765；它们不能保证全部剩余传输可执行。
- 每题 query/candidate/final 上限继续 16/12/1，按原工作台实际槽计。保存 inspect 的唯一首次应用若完成，首题 query 使用量就是 1，其余 15；没有完成就不得以“恢复”名义重执行未知槽。终态失败不重开、原一次 attempt 不补试，原第二传输纠错政策修订也不再消费第二次。
- 公开上下文继续完整 1,048,576 UTF-8 字节上限，不截断、不另开无历史同题；不承诺为 final 自动保留一次调用。一个合法 final、弃权或无效 final 均按原终态处理，之后按固定顺序进入下一原题，不能因初看结果好坏换题。

主控最后准入必须重新核实际计数；上述数字是本次已给定起点，不可硬编码成将来的余额。原 rubric 不改、不新增固定词表或答案要求。`costs_applied=false`、`execution_valid=false` 和正式策略/P4/净优势正控分母增量 0 均保持。即使四题最终全有报告，仍只是暴露开发材料上的描述性调查，不能宣称架构赢家或隐藏正控资格。

## 需要的最小工程，而非继续无界修接口

现有 v17 不能直接改个 runtime/resume 参数实现此设计：它在 [`casebank_continuation.py:129`](D:/大学/金融投资与量化/ai策略迭代开发/QuantaAgents/experiment_traces/meta_ashare_revision17/src/quanta_agents/meta/casebank_continuation.py:129) 核原预算/创建时间，在 [:707](D:/大学/金融投资与量化/ai策略迭代开发/QuantaAgents/experiment_traces/meta_ashare_revision17/src/quanta_agents/meta/casebank_continuation.py:707) 和 [:768](D:/大学/金融投资与量化/ai策略迭代开发/QuantaAgents/experiment_traces/meta_ashare_revision17/src/quanta_agents/meta/casebank_continuation.py:768) 核原模型准入截止；`allow_saved_application_after_deadline` 仅授权显式本地保存动作应用，不能挪作付费许可。

最小新增对象是一条同 registry/SQLite 的**补充阶段许可与关闭记录**，绑定原 plan/task/WB/所有历史模型及工具记录、原关闭快照、来源迁移和唯一新绝对窗口。复用原 dispatch lease；在同一 `BEGIN IMMEDIATE` 中核唯一 active 阶段、原全部 calls + correction + continuation 的累计费用/次数、新窗口、暂停和实际 task 状态，再追加新调用 intent/预留。原 v15/v16/v17 已冻结历史不得重写；新调用同时标原 task、累计 transport ordinal、semantic round 与 supplemental stage id。新许可必须独立批准，不能由旧 resume 或 paid=true 推导。

实现验收只补直接影响此边界的反例：过原截止旧入口仍拒绝、新许可仅新窗口内准入；双进程不能开两个窗口或重复第三传输；91,235→171,235 与第18/69次、原 cap/新 unknown 均严格阻断；保存 inspect 已应用时不重执行、未知 pending 不补算；完整本题 history/原 final 槽不丢。不要重新生成题库、换策略核或扩泛化测试。已有通过的工具/GUI 仅在实际依赖变化时复验。这样新增工程是一个资源治理边界，下一实质工作就是研究者的下一条证据决策。

## 能推翻推荐的证据

1. 实际来源、费用或任务状态无法闭合，存在第二个 unknown、未结工作台槽、在途旧 worker，或新旧总账不能同域原子计数：应先停机核对，不能借新时间窗绕过。
2. 同题输入/历史已泄露私有机制或人工答案，或要回答的是“严格六小时自主完成率/未见任务迁移”：同题补充不能支持该结论；应保留原失败，另行预注册真正未暴露任务和新独立评价预算，而不是将补充样本冒充原样本。新任务不继承“原样本成功”资格，也不能转移抹掉旧费用。
3. 补充阶段在正常可用接口下仍没有证据支持的 final/弃权，或持续出现同类合同失败：这是有价值的负结果。保存轨迹并据此决定工具/研究行为改进；本设计不再提供第三个自动时间窗口。反之，若首题形成准确且边界诚实的报告，才有依据继续评价原余下三题及之后的未见迁移，不能直接扩大为正式收益试验。

用户 [`META_FRAMEWORK_IMPLEMENTATION_PROMPT.md:13`](D:/大学/金融投资与量化/ai策略迭代开发/QuantaAgents/docs/META_FRAMEWORK_IMPLEMENTATION_PROMPT.md:13) 已授权合理研究和隔离改进，并要求防意外循环/重复付费；[:78](D:/大学/金融投资与量化/ai策略迭代开发/QuantaAgents/docs/META_FRAMEWORK_IMPLEMENTATION_PROMPT.md:78) 允许阶段冻结预算但禁止未知付费自动重试。这支持主控提出一次明确后续开发实验，无需把合理新研究一律视作禁止；它不覆盖本报告尚未形成的具体许可。原 scope 禁止自动扩展仍然有效，任何新窗口必须承认并冻结这次时间资源修订。

本次核验的文件身份：实施提示词 `dd8d995495d9082d0f1f5302c78764030338848d1f2d678b0946feb16ca4ea03`；原 rubric `381352961490781c5cc05f64ac835d57003e3587ab13bb7ec9815ee7ff8effbe`；原 budget `5e57b6cb9308fa3389c7aa102d24242a7825d2ce82cda37b15a9553fa2dcacd5`；原 scope `e72076081931c38629e4d096111f1eb156fa6add815789f3d2bbc4ecf404d8c9`；v17 设计 `feb326df05463f9a0a8a2fc8063e4e0642015f994d3dfaf8f6a2901e8a80bc14`；工程报告 `1a51e8c7f072d39ed7d9760ca8d682a59b7c6d47bf3e45d052f46502fc5b70f0`；核心源 `6cc0dc07cb60ac3f8f8b507b911c028a85a82305283132be4432a9b401b19b86`。

两问自检：**本次解决了什么，是否把到期改写成成功？** 只界定了可解释的后续实验；原六小时不完整、全部费用/机会和四题分母保留，新增时间明确作为额外资源。**下一项最有信息量的工作是什么，什么结果改变决定？** 先完成唯一保存动作的显式本地核验/应用，再在另获准入的一次固定窗口内继续原首题，观察能否形成证据支持的报告或合理弃权；真实负结果、泄露或审计无法闭合将推翻继续建议。本文没有生成执行 scope、ready/paid 许可或启动任何研究。
