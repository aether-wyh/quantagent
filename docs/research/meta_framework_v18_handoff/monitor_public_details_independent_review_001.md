# v18 公开费用与主控自检展示独立复核 001

结论：**该展示增量的源码差异及已保存定向证据有界通过。** 本次没有打开 actual 数据库、启动服务、操作浏览器、构造控制器或派发模型，也没有重跑作者 10 项 Python / 4 项 JS 或旧 52/15/29 检查。尚未验收新补充阶段投影或实际浏览器整合。

独立只读核验产物：`experiment_traces/meta_ashare_revision18/monitor_public_details_independent_review/001_saved_only/receipt.json`，SHA `51108c8a689feed1b8d62372498eeefa680a62da49556dc549a795ee379a44cd`。同目录 `verify_saved.py` 只读回执、源文本和测试产物，不执行运行时 API；实际退出码 0，观察耗时 0.96 秒。

已逐项核四份作者 receipt、其全部产物 SHA 和当前输入文件 SHA。保留真实历史：001 Python 在 pytest 前环境启动失败，执行数 0；001 JS 四项失败；002 Python 十项失败；003 十项 Python 全通过、四项 JS 全通过。JUnit 实际 testcase 与失败元素核对一致，未把不同运行拼成同次测试，也未将源码比较称为旧测试重新通过。最终 monitor 源为 `87d236c7cab3a6073fd1e5c1291f51c4263904651fb5873daace606705e948a4`，作者 003 receipt 为 `9ffc96f66d5ea768b53756bff03fea10114394cbdfac8b487e814ab602cb3932`。

独立 AST 比对确认已有 Python 定义仅变更 `public_calls` 与 `snapshot`，新增三个公开投影 helper；其余定义，包括 Handler、registry 边界和原回答/工具结果路由不变。原 v17 monitor 与保存的 before 源逐字一致。仅求值常量 PAGE 及字符串 replace 语句后，展开 HTML/JS 与保存文件逐字匹配；从新 JS 移除一段新增显示 helper 和两处明确的显示插入，所得整体 JS 与原展开 JS 完全相同，没有仅凭作者摘要认定异步身份逻辑未改。

功能判据与证据相符：耗时只来自保存回执，缺失显示未知，非法值不冒充有效秒数；真实 0 保留。已知 token 小计与未结名义预留分列，部分报告继续保留预留；旧记录没有名义值时不猜默认 80000。此处沿用未改变的 `call_budget_usage`，没有费用释放或预算修改。checkpoint 只投影四个公开文本字段，每字段 4096 字符、截断有标记；最新缺失不退回旧说明，嵌套和未知字段不出投影。JS 使用 textContent，公开说明与模型隐藏推理、验收/许可/存活声明有明确区别。

本结论仅覆盖以上固定源版本的展示增量。若新 supplemental 接线改变 source，应按实际受影响范围另外核对累计旧费用、新阶段身份和双窗口归属；当前 10/4 证据不能替代该整合。未执行真实浏览器检查，未宣称部署完成。工程模型用量未计量；项目 Gateway 调用为 0。

两问自检：**是否把未知值显示为零或把只读显示变更成预算许可？** 没有，保存字段与未知预留语义保持。**是否把原测试复跑或假浏览器当新阶段验收？** 没有，本次只读验证既有证据与源码，未给新阶段派发准入。
