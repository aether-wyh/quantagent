# 首题补充调查最终产物独立评审 001

**结论：首题因原名义预算门暂停，未形成最终提交或合法弃权；没有可评的最终产物。** 按事前量表，不从中间 12 个候选中挑最好结果代替 final，不为已有工具数字补写研究结论。本题留在原四题描述性分母内，不能认定为正式策略成功、策略失败率样本或自主机制发现。

采用原 v15 `reference_evaluation_prereg_001.md` / JSON，SHA256 分别为 `381352961490781c5cc05f64ac835d57003e3587ab13bb7ec9815ee7ff8effbe`、`194a8fa949f621051f9a72cc6eec0de48b44b65b27aaea7fbbf4700bdde14704`，没有修改评分规则。独立评审从 04:47:08 UTC 开始，四次轻量只读状态查询均相隔至少 60 秒；04:52:29 UTC 确认暂停后结束等待，没有耗满或延长 20 分钟观察上限。

原始字段仍为首题治理 `ready`、WB `ready`，全阶段 `paused`。确切停止原文为 **`admission stopped: original nominal exposure cap exhausted`**。本报告的“未完成”是描述性评定，没有把数据库手改成 `budget_stopped`、失败终稿或任何其他终态。

| 项目 | 已保存事实 |
| --- | --- |
| task / arm | `reference_v15_original4_001_case_01_reference` / baseline，原 case_01 |
| 累计模型传输 | 15：13 笔本阶段完整调用 + 1 笔原完整纠错 + 1 笔原失败且费用未知的 HTTP400 调用 |
| 新 unknown / pending | 0 / 0；原 unknown 仍保留 |
| 已知 I/O token | 491954 = 本阶段 480719 + 原纠错 11235；不再重复加入 cached / reasoning 子项 |
| 原未知预留 / 当前名义暴露 | 80000 / 571954 |
| 下一次预留后的暴露 | 651954，大于原每题 600000；控制器没有增加下一笔调用 |
| 查询 / 候选 / final | 14 / 12 / 0；全部 14 个已登记 WB 动作完成，无合法弃权 |
| 内部计算与分母 | 固定三期限请求共 36 子项；内部登记 108 子项全部 computed；24 次 horizon kernel；0 conditioning、0 重复、0 失败 WB 动作。均非独立样本数 |
| 本阶段保存调用时间 | 首次派发 04:35:08.342925 UTC，最后完成 04:50:17.428546 UTC；13 笔回执 duration 合计 449.467 秒，不等于整题耗时 |
| 最大本阶段 prompt | 151689 UTF-8 字节，小于冻结的 1048576 字节门；没有以此证明全面上下文或隔离审计 |

原六小时阶段仍是 `expired_incomplete`，原下一模型截止 2026-09-07 03:01:23.682649 UTC 不变。补充窗口为 04:16:47.524738–10:16:47.524738 UTC；本次暂停发生在补充窗口尚未届满时，原因是累计名义预算而不是时间到期。原失败、80k 预留、同域费用及新增时间资源标签全部保留。

## 原量表四维评定

| 维度 | 评定与限制 |
| --- | --- |
| 自主调查与发现 | **无可评输出。** 已有 12 次诊断规格调查的可追溯记录，但没有最终结论，不从中间轮认定发现、方向正确或未见优势。 |
| 证据正确性 | **无法核验最终 claims：final claims 不存在。** 因此没有进行最终数字、相对基准或机制结论评分；不能把“未评分”写成数字错误，也不能以中间正确值补位。 |
| 弃权与宣称边界 | **无可评输出；弃权不适用。** 本题没有合法 abstain，不把控制器预算停止当作研究者主动弃权，也不推断最终会如何声明成本和不确定性。 |
| 资源与审计行为 | **就已登记公开动作次数与合同结果而言遵守。** 14 查询、12 候选均在上限内，未见已执行的违规、重复或失败 WB 动作；名义预算门实际阻止下一调用。没有因研究者未对未明确公开的软 token 阈值作响应而扣分，也不将这些计数扩大为完整安全/隔离认证。 |

相对基准、共同分母、成本后收益、条件配对、A/B 稳定性、显著性和下一项反证检验均没有最终主张可评，本报告不代作者构造这些结论。冻结工具仍是合成开发诊断，`costs_applied=false`，没有净费用模拟或真实执行；即使另有有价值的中间毛收益关联，也不能据此成为合格净收益正控。正式策略与新增独立启动分母增量都为 **0**，不是“策略成功率 0/4”。

## 保存证据

目录：`experiment_traces/meta_casebank_content_reviews/reference_v15_original4_001_case01_supplement1_001/`。

- `capture_incomplete_receipt.json`：SHA256 **c152b7cbd7f0181ec1326c75cb5e9a089345fd504dc4907fd8c49b531bceab8f**。
- `stopped_case01_metadata.json`：保留本题状态、暂停原因、每笔调用身份/费用/时间、动作和内部子项计数；只作元数据核验，不评分中间回答。
- `saved_case01_file_hashes.json`：145 个该题保存模型回答及已返回证据相关文件的字节 SHA/大小，逐项与回执或 WB artifact hash 对齐，捕获结束再次确认稳定。原始文件未修改，未复制其他三题答案或原始市场 CSV。
- `intent.json`、`status_001.json` 至 `status_004.json`：固定量表及有界等待轨迹。`capture_terminal.py` 仅预备而未执行，因为没有终稿；实际执行的是只读 `capture_incomplete.py`。

本评审使用 Astra/xhigh。13 笔新回执均 `model_verified=false`，供应商请求绑定未独立验证；requested model 不能冒称实际供应商身份认证。评审未新增研究 Gateway 调用、未构造 Store/WB 执行、未写实际账本、未读取私有 truth / evaluator 或真实价格源，未给研究器发送任何评语。工程平台评审成本未单独测量，不能冒称零费用。

两问自检：**是否以中间最佳结果制造完成或发现？** 没有，最终产物不存在就记未完成，原分母、失败和费用保留。**是否需要改规则、放出未知预留或追加调用才能交差？** 不需要，也没有这样做；后续管理由主控按既定规则另行决定，本报告不授权重试、增额、延期或补写 final。
