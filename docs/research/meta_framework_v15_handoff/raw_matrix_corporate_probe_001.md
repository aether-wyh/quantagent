# 两股机械矩阵的公司行动候选补证 001

2026-09-07。Astra / xhigh 有界来源取证；本轮已关闭。**两次固定查询均成功、各返回一条候选记录；它们没有自动给当前机械账户增加一笔股利，也没有证明全部公司行动覆盖。** 未执行组合、读取价格、调用研究模型或修改旧计划 / 标志。

本地先检索 docs / scripts / src 和 experiment_traces 的 request、api、probe 记录，发现的 BaoStock 尝试仅为先前 `sz.000001 / 2019 / operate`，没有这两股的同请求。沿用已保存的官方合同与 0.9.3 SDK，wheel SHA256 `acbd19403285bc4e254cee8297cf0e2646ae2276e5af7e549deed3988ab02293`；本轮重新将本地 SDK 的 56 个包文件逐一与 wheel 原字节核对一致。未下载新依赖或改 SDK。

冻结计划：[plan.json](../../../experiment_traces/meta_raw_matrix_corporate_probe_v15/plan.json)，字节 SHA256 `67cfdb9769679e26cc4989bddfec2eca1069a41f17f444586d6e0bed5c51e339`。固定顺序 login、`sh.600004 / 2019 / operate`、`sh.600006 / 2019 / operate`、logout；最多 4 API，禁止重试、翻页、换股年份与备用发行人取证。socket 10 秒、每响应 2 MiB、累计原 TCP 参数与响应 6 MiB，外层进程超时 90 秒。下一调用准入截止为 20:27:48 UTC。

实际四项调用全部 error_code=0，20:25:46–20:25:48 UTC 完成；仅一个官方 SDK TCP 连接、4 次 send，各次实际发送数等于所保存参数字节数。原请求及响应合计 **1,616 字节**；两条分红响应分别 562、574 字节。进程 exit=0，外层耗时 3.531 秒。重试、翻页、价格请求、发行人备用查询、回测与研究 Gateway 均为 0。没有继续使用剩余时间或额度。

## 原返回与矩阵关系

官方合同的 `operate` 表示除权除息年份；`report` 表示预案公告年份，并非会计报告年度。以下金额是该候选行的每股税前股利；税后值按原字符串保留，不选取其中一个分支作为本账户实际税额。

| 字段 | sh.600004（白云机场） | sh.600006（东风汽车） |
|---|---|---|
| 预案公告日 | 2019-04-30 | 2019-03-30 |
| 股东大会公告日 | 2019-06-27 | 2019-04-23 |
| 实施公告日 | 2019-08-03 | 2019-06-13 |
| 股权登记日 | 2019-08-08 | 2019-06-19 |
| 除权除息日 | 2019-08-09 | 2019-06-20 |
| 派息日 | 2019-08-09 | 2019-06-20 |
| 每股税前股利（元） | `0.17` | `0.0833` |
| 每股税后原字符串 | `0.153或0.17` | `0.07497或0.0833` |
| 每股红股原字符串 | `0.000000` | `0.000000` |
| 预披露 / 红股上市 / 每股转增字段 | 三项均为空字符串 | 三项均为空字符串 |

全部 14 字段、分红送转说明原字符串、错误、请求参数与返回身份分别保存在 [query_1_result.json](../../../experiment_traces/meta_raw_matrix_corporate_probe_v15/query_1_result.json)、[query_2_result.json](../../../experiment_traces/meta_raw_matrix_corporate_probe_v15/query_2_result.json)。各为第一页一条，返回页容量 `2000`；未调用 `ResultSet.next()` 或其他翻页方法。第一页较短不等于独立认证了数据全量。

当前冻结矩阵为 2019-06-20 至 2019-07-11，零初始库存，首次计划买入 6 月 21 日，计划退出 7 月 2 日：

- **白云机场：**只有股东大会公告日落入 16 会话；实施公告、登记、除息、派息均在 8 月，晚于矩阵末日。若账户实际完成 7 月 2 日退出，则没有这笔 8 月登记权利；目前没有实际执行，不能从目标为零推断卖出已完成。若退出被拒或有剩余库存，8 月登记和后续权利仍需保留、明确右删失或另冻后续日历。8 月实施公告不能作为 6 月决策时已知信息。
- **东风汽车：**除息与派息日落在矩阵第一天；登记日 6 月 19 日在矩阵之前。按本计划 6 月 20 日零初始库存、6 月 21 日才买入，没有这条记录的登记权利，因此不能把 `0.0833 × 后买股数` 加入账户现金。它仍是识别边界日事件的有用候选。派息日期不证明当天开盘前账户到账。

两行的 `dividStocksPs=0.000000` 只支持该返回行显示红股为零；空的 `dividReserveToStockPs` 和上市日期继续是 unknown，不能推出所有送转、配股、合并、增发、更正、撤销或其他公司行动均不存在。现有数据也未认证原公告全文、历史实际可得时刻、具体账户税负或完整事件范围。

## 保存身份与下一步边界

证据包：[meta_raw_matrix_corporate_probe_v15](../../../experiment_traces/meta_raw_matrix_corporate_probe_v15)。32 个文件列入清单，另有清单文件本身；保存冻结脚本、启动意图 / 退出状态、stdout / stderr 原字节、每次 API 请求结果和逐段 TCP 原件。既有官方文档原 body、解码文本、HTTP 回执及 wheel 原件仅作本地复制，保留原来源身份，不计作新的 HTTP 请求。

- 网络探测脚本 SHA256：`bf57940077e59fe37970481d0af080e4dd6d3ff4dd68ccf500b92bf367b8c7b1`。
- 白云机场原 TCP 响应 SHA256：`b8b3d00b5945f86117c39ccc8ed470165f71afae039cd966754b3d10224902a3`。
- 东风汽车原 TCP 响应 SHA256：`e92b9d4c4606ac477caeb3e9d0ccae817b1bea3977ec115795e6e03dd6854249`。
- 候选范围分析 SHA256：`5691041269ac7c2184e76d84247cf337f25eb6d75ac99000f9a49c85224fc259`。
- 证据清单 SHA256：`99a40a2c4c27f7077abe77d34bdbbc59bfe8f2624646708666749df164d55c28`。

原 metadata-only 机械计划字节 SHA256 仍为 `dbe5c0ccbbf3bde5ca48167800af0e71a4acfa40e5fd4992a240cf68be95c318`；本轮清除 pending 义务数 **0**，192 pending 和所有 false 标志保持原样。候选结果可以给后续 controller 的范围说明提供依据，不直接授权执行或改写旧计划。

两问自检：本轮取得了原先没有的、指定两股同一固定年份的可复查公司行动字段，并定位一条登记早于初始账户、一条登记晚于计划退出的边界；尚未证明全部事件覆盖、实际退出、历史信息到达或账户到账。若后续可信证据表明登记日、送转条款或更正记录不同，或实际剩余库存延续至 8 月，须重新评估相应权益；不得靠忽略这些事实闭合资金链。本轮到此关闭，不追加来源或查询。
