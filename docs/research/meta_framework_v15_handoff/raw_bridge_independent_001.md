# 原股数持久桥接独立审查 001

2026-09-07。根代理按 Astra/xhigh 独立审查 `raw_saved_research.py` 与 v15 的原股数模拟器扩展；六项新增反例全部通过，未发现本次范围内未关闭的实质阻断。未调用外部行情、真实保存行情演练或研究模型。

源码差异限定为默认关闭的 checkpoint 包装、账本事件前后/拒绝回调、每日及终态回调。原手续费、持仓、税款、买卖和估值分支未改。作者已用公开算术 fixture 比较 v14、v15 默认与启用回调的完整 JSON，结果相同；其最后专组 24 项和此前旧模拟器 30 项回归不重复计作根代理新运行。

根代理复用合成输入构造器，独立设计并执行以下六种触发，预期金额由普通算术及保存状态推导，不使用回测函数生成预期：

1. 两个真实控制器实例并发进入：第一实例持有派发锁，第二实例被拒；只有一次输入加载和一次模拟。
2. 买入前的写入失败：没有进入买入账本更新，原 10,000 元现金保留。
3. 买入后记录已提交，但确认返回报错：保持失败状态，保存前缀包含 900 股和 985.82 元现金，不把该成交再次应用。
4. 计算完成但终态从未持久化：仅返回中断检查，保留终态写入预留；重复显式调用或 saved-only 恢复都不补算、不擅自制造完成收据。
5. 当日成交量使综合质量标志失败：仍按已冻结的此前信息决定开盘动作，没有用日末质量标志事后删掉买入。
6. 显式退出因容量拒绝，随后末日持仓缺收盘价：保存未退出的 900 股、985.82 元现金、拒单以及失败日期，NAV 只完整到前一日，不能删持仓或当作全现金。

买入算术为 10,000 − 900×10.01 − 5.00 佣金 − 0.18 过户费 = 985.82。它是合成会计断言，费用和成交模型仍有明确模拟边界，不是该证券的实测交易表现。

新检查实际为 `6 passed in 3.90s`，JUnit 和根代理收据保存在 `experiment_traces/meta_ashare_revision15/raw_saved_validation/root_independent_001.*`。收据绑定两个源码、作者构造器、独立测试及 JUnit 哈希。根代理没有重复运行作者已通过的 24 项或旧 30 项。

资源边界为最多 2 股、16 日、64 个事件意图、每份 checkpoint 4 MiB、总持久载荷 128 MiB；下一事件前为事件后记录和终态保留额度，未知写入继续占预留。SQLite 载荷额度不是包含数据库页面、文件系统元数据的磁盘硬帽。saved-only 回放的是已保存账本事件，不重新执行组合模拟、源读取或补齐缺失收益。

一次真实两股矩阵的 metadata-only prepare 已在暂停消息送达前完成并保留：原顺序 `sh600004/sh600006`、既有 16 会话、192 项义务全部 pending，记录表为空，未读取 rows/源 CSV、未 execute。此计划只准备可审范围，不能冒称数据、公司行动或执行资格齐备；不重新准备来隐藏其历史。

两问自检：持久资金链在并发、写入不确定、拒单和持仓缺价下保存了可验证前缀，默认交易逻辑没有因回调而变化；局限是仍属条件模拟，实际小矩阵尚未执行，来源义务没有变成已认证。下一步应先确认冻结矩阵的具体事件、状态和费用合同，再单独准入一次有界机械检查；任何重复更新、失去未结意图、删除失败资金或过度宣称成交认证，都将推翻对应工程通过结论。
