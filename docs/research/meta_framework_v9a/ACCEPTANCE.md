# V9A 开发与实际研究验收

2026-09-12，版本 `meta_v9a.1.0`，研究 `v9a_factor_20260912_r1`。

开发和有界研究全部完成；年度单因子 Pearson IC ≥ 0.10 的跨期目标未达成。独立稳定性、交易盈利均未证明。实际研究已关闭，不继续使用确认反馈搜索。

| 验收层 | 状态与证据 |
|---|---|
| 独立入口与工程 | 137项相关回归通过，0失败/错误/跳过，38.565秒；沿用旧因子/数据/固定Ridge/账本，核心不运行账户 |
| 真实研究 | 两开发批、96次尝试（48主+48控制）、69个唯一公式全部执行成功；27次重复保留 |
| 固定预测器比较 | 开发45+确认5，共50个逻辑比较；46个可评价，4个为已有基线特征重复，无新增信息 |
| 冻结确认 | 按预声明排序冻结5个候选，确认访问1次，2021—2024已曝光历史 |
| 年度历史目标 | 0/5达到2019—2024六年度各自单因子IC≥0.10；69个开发公式也无一同时满足2019、2020门槛 |
| 数值复现 | 实际缓存/原路径/单个/批量/历史截断等价通过；5包本机重导入逐值一致 |
| 独立审计 | 手写前三因子22,026项，独立B/B+F1模型与增量4,907项，最终全包65,910项；最后最大数值差2.22e-16 |
| 独立稳定性/盈利 | 未证明；没有新增独立留出、账户、成交、费用或容量验证 |

主任务实际为 `gpt-6-astra / ultra`，三个实际子代理均为 `gpt-6-astra / xhigh`。任务职责为生成控制、数值评价、独立审计；本地原会话快照核验通过。用量、快照时间及未知货币费用见[COSTS.json](F:/V9A_Factor_Research_20260912/COSTS.json)。缓存输入属于输入、推理输出属于输出，不重复相加；回执覆盖整个开发和审计团队，不能称每个公式的纯生成成本。供应商请求次数与货币账单未知。

## 研究结论

初批三臂主尝试各12个。最佳开发较差年IC依次为：现有库F1 +0.01265、规则扰动对应F3 +0.00015、新结构压缩收盘压力 −0.00021。初批结果和失败原件固定保留。随后只增加一次12主尝试的结构修订，新增22个唯一公式；额外结构预算不能与另两臂累计数字当作公平优势比较。

开发排序首位是路径一致性动量（60日路径），精确公式为：

```text
pct_change(lag(close, 20), 100) * (abs(rolling_sum(pct_change(lag(close, 20), 1), 60)) / rolling_sum(abs(pct_change(lag(close, 20), 1)), 60))
```

训练固定正向，其2019—2024年度Pearson IC依次为 **+0.02029、+0.05696、−0.01655、−0.03224、+0.02751、+0.00301**。对应年度RankIC为 +0.01768、+0.05455、−0.01419、−0.01793、+0.04349、+0.00950。开发表现未在后期稳定保持。

F1加入固定B后的开发配对ΔPearson IC为+0.02762，确认降至+0.00011、95%区间[-0.01704,+0.01700]。三个入选修订的确认预测器增量均接近零；预测器比较不能替代单因子目标。所有候选后期三至四个年度的严格top40相对全池5日毛收益为负；这仍是重叠前瞻分组诊断，不是可交易账户。

五个包保留精确公式、方向、参数、父代、完整分母、来源/代码hash、原始分数快照及复现证据。`implementation_status=executable`、`research_evidence_status=rejected_annual_0.10_target`、`trading_use_status=gross_group_diagnostics_only_no_account_validation` 分别登记。

## 实现与取舍

新增独立 `factor_research` 包，组合调用现有能力；未继承V9Study或复制账户引擎。程序展开参数、计账、缓存、比较、选择、冻结和导出；原生模型只在机制、结构提案、开发反馈修订和最终解释参与。Python `run` 重放保存提案队列，不会启动额外模型生成。

既有源码仅两处必要改动：包入口延迟加载旧工作流；原执行股票池函数原样移到共用模块、保留旧调用别名。执行池函数体AST等价及相关旧回归通过。开始时保护了5,630个文件，无缺失；最终再更新master及新增文档，未覆盖旧结果。此前测试失败和零数值初始化废止原件仍保留。

冻结协议为2015预热、2016—2018训练、2019—2020开发，后续2021—2024确认。未排名标签是固定锚复权open[t+6]/open[t+1]-1；每年末六个信号日净化，池内相关至少100股、每年至少200有效日、覆盖至少80%。公式内部截面排名和评价均使用历史沪深300信号日池。详情与限制见[使用说明](D:/大学/金融投资与量化/ai策略迭代开发/QuantaAgents/docs/research/meta_framework_v9a/README.md)和[方法边界](F:/V9A_Factor_Research_20260912/verification/METHOD_LIMITATIONS.zh-CN.md)。

研究计时墙钟630.14秒、CPU580.41秒，单工作进程，最大观测私有内存1.693GiB；工程测试、外部审计另计原件。实际完整存储和最新模型token以成本文件的时间戳为准。没有证明成本或发现能力优于旧版。

## 可复核交付

- [完整报告：逐年IC/RankIC、区间、覆盖、分组收益、固定增量和全部69公式](F:/V9A_Factor_Research_20260912/FINAL_REPORT.zh-CN.md)
- [机制最终复盘](F:/V9A_Factor_Research_20260912/FINAL_REVIEW.zh-CN.md)与[本轮目标拒绝记录](F:/V9A_Factor_Research_20260912/REJECTION.json)
- [全部365条年度证据](F:/V9A_Factor_Research_20260912/ALL_ANNUAL_EVIDENCE.json)
- [机器验收与实际成本](F:/V9A_Factor_Research_20260912/release_acceptance.json)
- [5个可执行但目标拒绝的因子包清单](F:/V9A_Factor_Research_20260912/study/bundles/manifest.json)
- [最终独立审计](F:/V9A_Factor_Research_20260912/verification/independent_final_001.json)
- [原生团队配置和用量](F:/V9A_Factor_Research_20260912/receipts/team_receipts.json)
- [可运行入口](D:/大学/金融投资与量化/ai策略迭代开发/QuantaAgents/scripts/run_factor_research_v9a.py)

2025行情、因子、标签或账户数值均未加载；允许的文件来源元数据和逻辑日期过滤不证明Parquet物理页完全隔离。2015—2024旧曝光不能洗去，上游到达时间和复权修订未认证。导出在同仓库本机通过，绝对源码路径仍是依赖，跨机器迁移未验证。旧V7/V8/V9仍关闭，未重开旧预算。
