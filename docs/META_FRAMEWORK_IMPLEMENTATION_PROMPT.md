# 元框架实施与持续研究任务提示词

请直接在以下现有项目中接手实施，持续推进，不要停在计划或建议：

项目目录：D:\大学\金融投资与量化\ai策略迭代开发\QuantaAgents

## 我的目标与授权

我的目标是：产出一套面向A股、内部研究统一使用 gpt-6-astra / xhigh 的系统。用户只提供普通自然语言想法和正常研究文档，系统就能自主理解、调查、开发、回测、诊断、改进和交付策略，并在严格冻结的样本外评测与后续纸面运行中，稳定产生净夏普严格大于1的策略。

请实施本项目已经完成的元框架改进方案，并持续监控、修复、实验和完善系统，直到有足够证据达到目标。不能以某次开发回测超过1、工程测试通过、提示词写完或报告生成，宣布完成。市场未提供可发现优势时可以弃权；不能保证每个普通想法都能盈利。

我授权你在本目录内进行必要的可逆代码修改、隔离修订、测试、研究实验和本地GUI改进，并维护持续监控。不要为日常实施、负结果、合理实验或每个阶段重复索取许可。正常研究大量消耗token是可以接受的，风控主要防止意外循环和重复付费。保留事故预算与停止机制，不启动无边界的付费重试。不要实盘下单、动用资金、购买服务、向未授权的外部渠道发送私有数据或擅自扩大敏感权限。

如果环境支持目标跟踪，沿用本任务未完成目标；没有时建立上述目标。持续推进到真正达成，或明确需要新的数据、用户输入或外部状态才能继续；不能用无限重跑掩盖证据不足。等待新增数据时保持正确的未达标状态和有意义的监控，不伪造后台工作。

## 接手时先核验现状

按以下顺序阅读，结合实际代码和最新运行状态，不把这份交接快照当成永远最新：

1. output/reports/meta_framework_redesign_20260906.html：完整研究和实施方案，包含原始文献与设计边界。
2. docs/META_ITERATION_JOURNAL.md：尤其第09项及后续新增记录。
3. docs/META_REVISION6_INTEGRATION_REPORT.md。
4. docs/META_ASHARE_PROTOCOL_DRAFT.md 与 docs/META_ASHARE_BENCHMARK_CASES.md：沿用其中仍有效的正式验收口径，区别草案政策与已实现功能。
5. experiment_traces/meta_ashare_v6/batches/q_20260906_01/report.json，以及 experiment_traces/meta_q_gateway_audit/20260906T143902468130Z/audit.json。

然后按实施需要读取 META_ASHARE_DEV_BATCH_01_REPORT、META_RAW_PORTFOLIO_SIMULATOR、META_L2_DAILY_FEATURES、META_EXECUTION_DIAGNOSTICS_REVIEW 等报告，并检查相关源码和测试。

2026年9月6日23:03香港时间核验的交接状态：

- 新GUI为 http://127.0.0.1:8771/ ，新账本目录为 experiment_traces/meta_ashare_v6。方案页面在 http://127.0.0.1:8772/output/reports/meta_framework_redesign_20260906.html 。网页不可用时直接读本地文件。
- 第六版冻结包 experiment_traces/meta_ashare_revision6 的源码哈希为 46608b307615ab87422ad95e6835b7ff882d813a7f3dd550c50a0b2713e81228。已有659项联合检查和12项GUI检查通过，真实运行仍暴露了未覆盖的网关缺陷。
- Q批次 q_20260906_01 当前paused，3个运行failed、最后1个paused；4次实际模型调用共81,279已知token。三个失败调用实际已有完整回答、最终完成事件与相符用量，涉及61,758token；网关把已经恢复的中途重连错误误判为终态失败。
- 不得付费重跑这些失败槽、手造完成回执或回写旧失败为成功。当前没有合格的公开导入恢复接口。先在隔离修订中用已保存事件验证修复，再设计带来源审计的恢复流程。恢复若成立，也不是新的独立研究样本。
- 旧8次开发运行7完成、1失败，共3,262,406token；最高开发正式提交夏普约0.4536，且原研究执行方式不能认证正式收益。没有稳定达标结果。
- 2017至2021年是开发期；2022至2023年确认结果已查看；2024至2025年在当前元框架流程中封存，不能声称全项目或预训练模型从未接触。真实最终验证还需要前瞻数据。
- 原价组合执行只有有限样本验证，公司行动覆盖不完整。L2已做有限特征审计，尚无策略收益证据，现有短历史不能冒充长周期正式验收。
- 旧8769服务曾因自动审批拒绝停止而保留，返回原因仅为 blocked by policy。不要用其他命令绕过拒绝去关闭它；必要时使用独立端口和版本。

同目录可能存在预先未提交修改、其他任务、worker和监控。先核查并取得唯一执行权，保留他人改动；已有工作正在推进时先协调交接，不重复启动模型或调度器，不强停其他任务。现有每小时监控ID为 a，原目标任务为 01a071b1-31c5-7103-a46e-5c1859a5560f。接管后检查它的最新配置，更新或迁移已有监控，避免两个主控同时工作；不要盲目新建重复自动化。

## 实施顺序

P0 先修可靠运行。保持主源码与既有冻结实验完整，在本项目下创建下一隔离修订，沿用项目版本与来源核验方式。按请求和轮次区分重连中间事件、真正失败、最终完成、后续新轮。成功必须核对请求身份、完成事件、保存回答、schema及用量；独立保存退出码，未知就未知。用保存事件离线验证正常完成、重连后完成、真实失败、截断、多轮、内容不一致和用量缺失，不产生新模型费用。建立先登记意图与预留、后核对回执、再应用结果的恢复流程；本地幂等键不能代替供应商端执行证明。补真正的批次暂停和连续同类异常断路器，暂停后阻止新派发并妥善收尾在途请求。普通成员暂停不等于可靠批次暂停。

P1 建立校准题库，并为收益评分接通合格的最小真实日线执行链。首轮可采用8个开发任务：2个可判定诊断、2个隐藏可发现机制、2个无信号或机制失效、2个真实开放研究。正控参考解必须在相同信息、工具、成本和约束下独立复现，答案与生成器对研究器隔离，允许不同有效解法。负控明确相对什么基准没有预测优势。真实题不假定一定有alpha。改变随机实现和机制族，避免只记住模板。分别评估发现率、误报率、正确弃权、执行有效性和收益，不合成容易投机的总分。执行条件未合格的任务只能测诊断或流程，不能计入策略成功率。

P2 补研究工具。优先实现事件后的收益期限曲线、条件分组及匹配对照、实际持仓和成本归因；随后支持受控的因子与策略代码开发。明确最早数据可用时刻、共同样本、重叠事件、覆盖缺口和实验局限。对原成交路径的成本分解与改变交易策略后的反事实重跑分开。工具内部所有参数扫描、候选与失败也登记，不能把一次批量调用记成一次独立尝试。快筛与正式执行在共同样本上校准，抽查快筛淘汰结果；正式收益必须来自可信执行器。

P3 实施研究循环和GUI。采用可信实验内核、可变研究harness、独立元评测器的权限分离。研究器可以观察、联想、重解释旧证据、开发组合或提出竞争假设，再选择能增加信息的实验；不要求每轮机械列出固定数量的假设，不把低功效的无显著差异当作反证。主研究agent保持上下文，明确独立子任务才调用专家，所有实际研究、元研究、摘要与模型评审统一Astra/xhigh。记忆保留来源、适用条件、反证及失效记录，按需检索；跨评测运行隔离。研究器不得更改内核评分、留出权限或预算。

GUI持续显示公开返回的推理摘要、研究理由、工具交互、产出、活动假设及反证、实验分支、转向或弃权原因、正式提交、数据/架构版本、全部token和运行时间。它不能冒充模型完整隐藏思维链。增加批次控制、未知费用预留和异常原因；大型诊断按需加载，避免渲染数百万字符阻塞。用真实浏览器检查关键流程，不能只凭静态测试宣布GUI正常。

P4 比较架构。主轨道固定Astra/xhigh、任务信息、数据、工具和全栈资源规则，允许自主分配研究动作；窄消融轨道才固定诊断/回测/提交机会。主动多回测可以属于整体架构效果，不能自动称为比较无效，但也不能据此证明诊断能力提高。Q只能回答固定条件下的问题，不能直接与旧批次作单一改动因果比较。保留强单agent和固定harness简单搜索基线，候选选择权限相同。工具内核共同升级后，再单独比较组织流程。

先完成两道校准题的小闭环，再考虑8题×2架构×各1次的16次开发筛查；该筛查仅查工程和错误类型，不宣布赢家或估计稳定性。随后按事前固定的机制分层规则追加完整配对；没有独立校准重复时，不用每题一次的数据判断哪题波动大。实际样本量依据值得采用的最小改进、波动和相关性确定。配对交错运行，完整记录失败、弃权与全部成本。一次完整研究的最终合法提交才是评估单位，中间最好结果不替代最终提交。

P5 正式验证与持续运行。冻结架构、任务、选择规则、代码、数据、风险和评分后，检验未参与设计的任务家族和后续市场时期。任务留出与时间留出分开，所有人或agent看过的分数、轨迹、人工建议都进入反馈暴露账。正式结论出来后修改系统，该次结果成为后续开发信息，不能反复用同一留出挑战直到通过。

日线策略使用原价、实际股份、公司行动、现金、T+1、整手、停牌涨跌停、当时可见的股票池、历史费用和保守容量约束。补齐覆盖后再扩大正式范围，不删除持有中发生的不利事件。L2先验证字段语义与时间可得性，再与日线在共同股票、日期和成本上比较增量，不能把短期事件收益直接当可交易夏普。纸面运行按冻结规则监控失效，触发新研究时生成新版本，不回写旧收益。

## 成功标准与防止自欺

先复核现有正式协议，将仍属草案的数值政策在看新结果前明确冻结。默认沿用以下目标口径，不能为达标放宽：完整资金逐日净收益、含现金日及全部成本；年化252日、样本标准差、明确2%年化无风险基准，另报零无风险敏感性；净夏普严格大于1。风险、容量、交易数和覆盖门必须同时通过，零波动、无交易、样本不足都不算达标。

现有建议政策包括最终期至少252个真实交易日、至少30个退出批次、最大回撤不超过25%；正式首批至少6个真实留出案例、3个机制家族、每题每架构3次独立研究启动。这些是实验政策与最低起点，不保证统计功效；合成正负控不混入真实策略成功率分母。

“本冻结批次初步稳定达标”至少同时要求：候选有效成功率不低于2/3，有效策略夏普中位数大于1，预注册单侧成功率置信下限超过1/2，且工程、执行、风险和容量合格。区间方法须尊重任务族、同题重复及共同市场日期的相关性，样本不足就明确不足。“架构改进”另外要求相对强基线的配对成功率增益有足够证据；达到策略门槛与证明架构改善分别报告。初步批次通过后还要有冻结后的新增数据/纸面执行证据，才能讨论最终目标达成。前瞻验证启动前冻结最小覆盖、有效交易要求、收益风险、执行偏差及通过/失败/继续观察的判据；具体数值依据协议和检验目标确定。覆盖不足或只观察到短期高分时，只能报告阶段通过，不能宣布最终目标完成。

正式自主评估的输入只有普通定义和正常文档，不提供MA5答案、隐藏参考解、其他架构的轨迹或人工解题技巧。用户补充技巧的运行标为人工辅助，不能冒充自主能力。至少覆盖从普通定义开始的开发和面对失败证据继续迭代两种任务；后续还需检验普通输入表述变化与未见文档的迁移。

区分研究重启次数、市场时间块和机制家族，不能把它们简单相乘当独立样本量。DSR/PBO等只作有适用边界的独立审计，不当优化奖励，不解释成未来盈利保证。所有内部搜索暴露都保留；PBO分析单位需区分完整引导搜索最终产物与中间步骤。诚实弃权是合法研究终态，但不是成功产出策略。

## 预算 监控与工作方式

保持Astra/xhigh，不用小模型或低effort代理行为评测。工程fixture、保存输出重放、数值复算和协议检查优先零模型完成。实际请求配置与供应商返回身份分别登记，供应商未证实就标未验证，不静默降级。

按阶段冻结宽松预算，记录规划、摘要、专家、评审、失败和元设计的全部用量与耗时。可参考每运行30万token软提醒、60万名义事故阈值，但须根据校准成本调整，区分派发阈值、在途预留和可能超额，不虚称绝对上限。模型正在正常深度推理时不要因短超时打断；未知付费不自动重试。按预注册阶段扩样，不因一次高分或低分随意改规则，也不因为用户允许大量消耗就无限循环。

维护唯一的主控和监控。需要跨会话继续时使用持久检查点及现有自动化，先核验实际运行再恢复，避免重复派发。正常运行和未变化的已知问题保持安静，只在重要完成、明确异常、阶段结论或确实需要我输入时通知。停止等待、调度和恢复能力必须实际实现，不能只承诺“会继续监控”。新自动化需优先更新现有记录；当前有其他主任务运行时只读并协调。

每次有意义的改进后自检，并写入持久日志和GUI：
1. 这一步做得怎么样？预期与实际是什么，哪些证据支持或反对，解决了什么、引入了什么退化，还有什么未知？
2. 下一步该做什么，如何改进？下一项最有信息价值的工作是什么，不同结果会如何改变决定，预算与停止条件是什么？

不要让自检篇幅或角色数量成为奖励。优先完成必要检查，证据通过后继续下一阶段，不反复跑无关大测试。Windows长回测注意内存与进程生命周期，所有后台服务隐藏启动。研究环境的Python3.10可能因GBK的.pth配置不兼容全局UTF8启动，不盲目设置PYTHONUTF8或-X utf8；显式处理文件与输出编码。

现在开始：读项目和交接材料，核验主控及Q暂停状态，给出简短的当前判断，然后实施P0并完成离线验证。之后按上述顺序持续推进。每次交付清楚区分已实现、已验证、仍是设计和未达标部分；不要把新的计划当成完成，也不要在关键证据不足时宣布已稳定产出夏普大于1的策略。
