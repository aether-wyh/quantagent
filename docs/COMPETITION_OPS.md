# 比赛日更流程（兴证全球杯 量化赛道 · 子账策略 c2，c1 与集中式为影子臂）

建立日期：2026-09-16。策略与数字的来源见 `docs/research/framework_review_20260913/A14_COMPETITION_LOG.zh-CN.md`（第一步 1a—1j、第二步 2a—2h），冻结版本在研究根 `competition/frozen/c2_396_enhanced_turnneutral/`（主线，含 c1 与集中式影子臂的重放）与 `c1_396_enhanced_v2/`。

## 策略一句话

已冻结的全 A 396 个日频成员 LightGBM 打分（5/10/20 日标签融合，逐年扩展窗；比赛期用训练到 2026-04 标签的最终模型），限制在沪深300∪中证500∪中证1000 成分内；分数先在并集内取百分位并做 5 日平滑，再在**各桶内按 20 日均换手率五分档重新取分位**（换手中性）；中证500 桶 88%：按流通市值权重持有可交易成分，中性分位低于 0.20 的剔除、回到 0.35 以上恢复，不超配；其余并集桶 8%：中性分位前 25 名进入、跌出前 75 名卖出；现金 4%。基准中证500 价格指数；扣费按每边 0.2%。影子臂：c1（不做换手中性）、集中式 55/3/5、集中式 55/3/5 用换手中性标签模型、**c3-A**（同 c2 分数与 500 桶规则，分配 85/13/2、其余桶前 110 名保留 4 倍；A16 换版候选，目录 `targets\shadow_c3A_enhanced_85_13_n110`）、**c3-B**（换手中性标签模型 + 集中式 80 进 / 保留 320 + 84/14/2 + 其余桶 55 名；目录 `targets\shadow_c3B_turnlabel_conc80k4_84_14`）（A15 第 1 条，2026-09-16 起并行记录：训练标签先在换手五分档内排名，研究期集中式 +9.7%~+9.8% / 夏普 1.24~1.27 / 六年全正；前向 2025 −1.5%、2026 年 1—5 月 −17.9%）。

**A17 新增并行目标（2026-09-17 起，均不影响主线 c2；生成失败只记日志、不中断日更链）**：
- `targets\shadow_c3A_grouplots_85_13_n110`：c3-A + 分价格段的整手取整（不足半手的高价股按 80 / 160 / 320 元分段保留整手，先留已持有、再按指数权重从大到小）。
- `targets\shadow_c4_turnlabel_iw_o1000`：**c4 换版候选** = 换手中性标签模型 + c3-A 规则 + 用价格反推的指数真实权重（日更链每天先重估一次乘数，写到 `F:\A_Layer_Live\indexw`）+ 其余桶只从中证1000 里选 + 分价格段整手取整。冻结目录 `frozen\c4_396_turnlabel_ens5_iw_o1000_85_13_n110`（研究期 +6.0% / 2.19，2025 +0.5%，2026 到 09-16 +7.8% / 1.46；200 万整手 +5.3% / 1.87、−0.4%、+6.2% / 1.06）。
- 实盘目标生成模块的三个新开关（`cap_mult`、`oth_universe`、`lot_group_below`）默认关闭；关闭时 c2 的目标文件与改动前逐股一致（2026-09-17 回归验证）。
- **成交时点**：必须参加 9:15—9:25 集合竞价；分钟库回测显示拖到开盘后半小时每年少约 0.5 个点，拖到收盘少 0.7 ~ 1.0 个点。
- **12 月调样**：生效日为 2026-12-11 收盘后（12-14 起按新名单）。当天收盘后核对数据源里的中证500 成分是否已切换；11 月初可用调样预测脚本出"预测调出名单"，作为生效前的追加式否决（研究期 +0.3 个点 / 夏普 +0.08，可选）。

## 每日时序

| 时点（北京时间） | 动作 | 命令 / 产物 |
|---|---|---|
| 15:10 后（收盘 K 线落定） | 数据日更：指数、全 A 增量日线、成分、拼源 | `bash scripts/competition_daily.sh --nav <子账净资产> --holdings <当前持仓 csv>`（含数据步）；数据截至写入 `F:\A_Layer_Live\data_asof.json` |
| 随后（约 30—40 分钟） | 窗口面板 → 396 成员排名 → 打分 → 目标持仓 | 同一脚本；产物 `F:\A_Layer_Live	argets\c2_enhanced_turnneutral	arget_<日期>.csv/json`（主线）、`targets\shadow_c1_enhanced\`、`targets\shadow_concentrated\`、`targets\shadow_concentrated_turnlabel\`（影子臂；最后一条用第二个最终模型 `models\m396_allA_turnlabel_through202605` 打分，缺该模型时自动跳过） |
| 17:30 前 | 复核目标持仓的预检；写日报所需数字 | `target_<日期>.json` 的 `precheck`（买单 100/80/50/0% 成交四情形）、`target_invested`、`target_share500`、`target_max_single`、`n_orders`、`lot_rounding_abs_error`；**风格暴露** `style_exposure`（当日原始分数对 20 日均换手 / 对数市值 / 20 日波动 / 20 日动量的截面秩相关，500 内与并集内各一组；`csi500_cap_corr_20d_mean` 为 500 内分数对市值相关的 20 日均值，`csi500_cap_corr_20d_alert` 低于 −0.3 时为 true，只通报不切换） |
| 次日 08:50 前 | 把 `target_<日期>.csv` 的 `exec_shares` / `delta_shares` 交给统一执行程序 | 只交本子账的增量；按"先卖、先买 500 桶、再补其余桶"的顺序，收盘前复检 500 占比与仓位 |

持仓 csv 格式：列 `code`（SH600000 / 600000.SH / sh600000 任一写法）、`shares`（可选 `market_value`）。没有持仓文件时视为空仓（建仓日）。

## 目标持仓文件字段

`bucket`（csi500 / other / outside=须卖出的非池名字）、`code`、`score_pct`（并集内平滑分位）、`state`（排雷式：core / excluded / overweight）、`held_mv`、`target_mv`（规则目标市值）、`exec_shares`（按最新收盘取整到 100 股并做预算回削后的执行股数）、`exec_mv`、`delta_shares`、`delta_mv`、`action`（buy / sell / trim / hold / skip_lot）、`close`（参考价）、`is_st`。

## 约束与预检

- 目标仓位 96%、500 占比 88%、单股上限 9.5%（历史重放最大 2.4%）；`precheck` 里任一情形 `ok=false` 表示该情形下会越线，建仓日的 80% 及以下成交情形必然越线，因此建仓按 500 桶优先、分批补齐。
- 200 万子账上排雷式篮子约 360 只名字，目标金额不足半手的名字被剔除（`n_skipped_below_half_lot`），其金额按"每个名字最多补一手、目标大的先补"分给同桶其他名字（2026-09-16 A15 第 10 条起；此前按比例重分，会在连续多日里把金额堆到少数名字上），整手后总额不超过桶预算；取整误差约目标的 20%（`lot_rounding_abs_error`），这是该路线的执行代价。
- 分数缺失的持有名字保留；离开成分的名字强卖（`bucket=outside`）。

## 登记卡数字（从冻结目录直接填）

| 登记项 | 数字 / 位置 |
|---|---|
| 股票池 / 信号 / 持有周期 / 调仓 / 重训 | 上文"策略一句话"；持有周期由滞回决定（研究期日换手 2.6%，等效持有约 19 个交易日）；不定期重训按登记规则每月一次（训练到最新可用标签） |
| 样本外 / 留出段 | c2 研究期 2019—2024：六年净超额 +4.9%、净夏普 1.50、最差年 +0.9%、回撤 −5.0%、十日胜率 67%（**200 万净资产整手取整口径：+4.5% / 1.30 / 最差年 +0.6% / 回撤 −5.0% / 十日胜率 63%**，A15 第 10 条最终版）；前向 2025 −1.9%（毛 +1.8%）、2026 年 1—5 月 −2.4%（毛 +2.2%，年化）；c1 对应 +5.9% / 1.21 / −3.2% / −12.1%；纯跟踪篮子前向约 −3.9% / −3.4% |
| 十个交易日累计净超额 | c2：点预测 +0.1%，80% 区间 −1.1% ~ +1.0%（2026 冻结期 1—9 月重放：均值 +0.27%、p10 −0.91%、p90 +1.85%、胜率 58%——比赛期环境更像 2026，区间按此加宽为 −1.9% ~ +2.1%）（研究期均值 +0.20%、p10 −0.59%、p90 +0.94%、胜率 67%；前向 2025 −0.04%、p10 −1.09%、p90 +1.02%；2026 +0.12%、p10 −0.70%、p90 +1.05%） |
| 成本 / 换手 / 回撤 / 风格偏离 | 每边 0.2%；日换手 3.5%；净值回撤 −5.0%；换手风格已中性化；分数对市值秩相关 2026 −0.27 作风险指标逐日记录（20 日均 < −0.3 时通报） |
| 代码 / 数据 / 种子 | 冻结目录 `rule.json`；模型 `F:\A_Layer_Live\models\m396_allA_through202605\model_meta.json`（种子 0、标签到 2026-04-22）；数据截至 `data_asof.json` |
| 执行参数 | 行情：日线，收盘后；订单：次日开盘限价（参考收盘 ±2%）、有效期当日；限频：≤ 400 单/日（建仓日）、平日约 30—60 单；连续拒单 ≥ 5 停止并人工接管；回撤告警：十日累计净超额 < −1.1%（80% 区间下沿）通报，< −2.2% 停止新增风险订单 |
| 合规过渡方案 | 无法出目标时沿用前一日目标；数据缺失当日不交易 |
| 已知局限 | 前向两段净超额为负；成分名单 6 月以后的临时调整近似；手数取整误差；vendor 与公开接口两源拼接（重叠段一致性检验见 `F:\A_Layer_Live\README_data.md`） |
