# V3 实际模型校准与终稿恢复

2026-09-07。普通输入已经经过真实模型选工具、编写受控策略、全额资金执行和模型终稿，形成了可核对的证据链。当前只能确认这个工程路径可达，尚不能确认其稳定交付，更没有证明真实成本后冻结样本外净 Sharpe 严格大于 1。

## 本轮结果

| 范围 | 实际调用 | 已知 token | 模型最终产物 | 资金结果 |
|---|---:|---:|---|---|
| 原始成本负对照 | 6 | 202,214 | 真实弃权终稿 | 10,000 → 9,808.44 |
| 原始价格与活跃度 | 6 | 159,365 | 上下文超限，未交付 | 两条规则均 10,000 → 9,981.94 |
| 同证据单次终稿恢复 | 1 | 31,540 | 真实弃权终稿 | 不重新计算策略 |
| 合计 | 13 | 393,119 | 原始终稿 1 份、恢复终稿 1 份 | 3 次候选执行全部保留 |

原始交付率是 **1/2**，恢复成功单列为 **1/1**，不可合并成原始 2/2。独立真实市场样本数、正式策略成功数均为 0。所有模型请求固定 `gpt-6-astra / xhigh`；已核对本地请求、真实完成事件、输出格式、退出状态和 I/O 用量，但供应方返回未独立证明模型身份，13 次的 `model_verified` 均为 false。

本轮新增未知费用预留为 0。旧 V2 的 491,954 已知 token 和 80,000 未知预留另账保留；没有重开旧研究或重发未知请求。编写代码所用的当前对话费用不混入研究 Gateway 用量。

## 资金事实及研究结论

成本负对照的模型编写了 `where((activity > 3) & (close > 0), 0.45, 0.0)`，执行 16 笔成交、8 次往返、无拒单。价格毛利为零，滑点 80.00 元、佣金 80.00 元、印花税 30.36 元、过户费 1.20 元，合计净亏 191.56 元。期末股票数量归零，初始 10,000 元未缩减，12 个交易日均保留。

价格与活跃度案例先检验活跃度加权涨跌幅，随后尝试活跃度加权反转，再删除活跃度做价格反转对照。两条规则均成交 12 笔、无拒单，期末股票数量归零，净亏 18.06 元；费用 115.06 元、滑点 156.00 元。根据保存的成交路径，价格毛利为 253.00 元，未覆盖 271.06 元费用与滑点。该分解没有重新模拟无成本策略，也没有再次扣除已包含在成交价中的滑点。

独立离线核对进一步确认，两条规则的逐笔成交和每日资金记录完全相同。恢复模型谨慎地只认定汇总相同，并明确自己没有在恢复调用中重新查阅完整逐笔页；原始模型文字保留，不由控制器补写这一结论。合成信号结构没有被证明发现，不能把合法弃权评为发现能力通过。

三次执行的原始保存记录均通过 `reconcile_saved_only`，另用十进制重新核算成交现金、费用、滑点和股票数量；没有重开价格 CSV、重新执行候选或追加付费核验。结果属于合成、声明成本的机械校准，执行认证、真实容量、历史到达时间和正式金融目标均保持 false。

## 实际失败与修复

原始 `inspect_execution/failure` 将整个重复执行快照放入返回值，后续提示随之增长。负对照最终调用已达到 249,159 UTF-8 字节、94,640 实际 token，超过单次名义 80,000 预留；实际费用完整入账，名义预留从未当成供应方硬上限。

价格与活跃度案例的第 7 次调用在保存请求、付费发送之前触发 262,144 字节上限。前 6 次均完成且费用已知，进程退出，原始范围暂停关闭。保存旧源码快照和失败记录后才修改下一版本。

修复对每个历史动作保留身份和状态，对大结果使用注明不完整的、有原始哈希及字节数的首尾摘录；不会修改原件或把省略字段当作不存在。失败查询返回状态和原件定位，避免嵌入完整重复快照。另修复无 raw 子执行时的空值读取，研究入口脚本加入源码指纹。

恢复计划只允许一次 `submit_research_report`，不准继续试验，复用本任务原始普通输入和六条已结算历史。保留原截止时间 `2026-09-07T14:18:34.605004Z`，仅分配原任务与原阶段剩余额度内的一次最终机会，独占恢复声明阻止复制预算。恢复提示为 68,582 字节，实际调用完成，终稿明确承认被摘录省略的证据及本轮局限。

## 验证与可复查产物

- 原子账本、保留最终机会、共享预算、并发争用、未知费用阻断、应用恢复等最初 16 项通过。
- 普通输入到资金执行和终稿的 4 项集成检查通过；之前由测试回调嵌套事务造成的 4 项失败也保留。
- 修复回归先得到 21 通过、1 项测试断言失败：测试错误地期待排序后对象的现金字段位于尾摘录。改正断言后，两项上下文测试通过。共 22 项不同检查已有通过证据，不将它描述为单次 22 项全绿。
- 浏览器已核对本地面板显示 393,119 token、13 次调用、2 份已保存终稿、原始失败和恢复分别呈现；展开恢复终稿可见真实模型结论与局限。

原始审计：`experiment_traces/meta_framework_v3/calibration_001/audits/saved_001/audit.json`。

负对照模型原文：`experiment_traces/meta_framework_v3/calibration_001/audits/saved_001/calibration_cost_001_model_report.md`。

恢复审计及模型原文：`experiment_traces/meta_framework_v3/final_recovery_001/audits/saved_001/`。

本地只读面板：[QuantaAgents V3](http://127.0.0.1:8777/)。该面板没有启动研究或控制实盘的接口。

## 尚未完成

目前公开执行适配器仍限于合成校准。真实两股、16 日的 192 项执行义务未闭合；846 股 × 1,217 日的既有库存没有重扫。已取得两份发行人 2019 年年报并人工检查股本变动页，作为事后公司行动补证，尚未据此解除任何 pending。

下一段工作应先完成限定两股窗口的公司行动和参考价格证据，接通已有真实保存数据与 V3 工具；继续使用全额资金和明确的模拟/认证标志。然后按原协议冻结新的真实案例与公平重复，不将本轮合成数据重新包装成独立验证。跨案例、机制家族、独立重复、统计门、风险、容量与冻结后新增数据/纸面验证均未完成。总目标保持进行中。
