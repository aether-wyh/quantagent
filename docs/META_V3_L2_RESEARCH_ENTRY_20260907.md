# V3 盘口资金执行接入研究入口

2026-09-07。盘口账户现在能接收受控策略程序生成的目标，并把完整现金、实际匹配股份、分红、税款、拒单和失败结果传回现有研究工具。最终相关检查 **35 项通过**。保存的八股真实盘口经该入口产生 **8 条来源拒单、3 个完整现金日**；100 万元参考本金保留，`portfolio_pnl=null`。

本次实际运行使用事前固定的控制器配仓输入，专门验证接线；没有新增研究模型调用，也没有生成 Astra 研究终稿。它不能计为新的策略研究成功、独立样本外案例或零收益策略。正式目标仍未达成，正式案例分母仍为 0。

## 本次接通的行为

`ResearchTools.develop_strategy` 新增显式后端 `v3_l2_cash_001`。沿用原受控表达式与因果目标生成：前一会话形成目标，下一会话按事前固定的 09:35 时点下达限价 IOC 情景订单。股份数量由先前完整净资产和下单前可见盘口确定；订单数量在查看执行盘口之前保存。拒单后不重新分配目标，也不使用稍后卖出所得为同一决策时点的买单提供资金。

原日线开发入口继续使用原日期和执行限制。新的盘口输入明确登记为已暴露的 2026 年开发资料，未打开封存的 2024—2025 年资料。当前盘口输入不提供经过验证的日线开盘价序列，因此不开放日线开盘端点诊断；其他日线任务的诊断工具保持可用。

研究器可以按页读取每日账户、订单与拒单、失败时最后可验证日期及原始部分结果。紧凑视图保留每行金额、股数、价格档位和剩余数量；嵌套来源与税款明细保留在原件。分页遵循返回的 `next_offset`。全部拒单的运行状态为 `completed_execution_evidence`，不能作为已完成资金执行的候选提交；研究器可以依据它提交弃权。

持仓缺价会停止数值路径，已买入股份、费用、登记权利和最后完整现金日继续保留。研究后端不自动恢复或重跑已开始的范围。

## 公司行动如何进入盘口账本

复用冻结的现金分红适配器，由已经准入的公告事实和实际账户状态生成事件。登记日实际持仓决定权益，部分成交产生的多个批次保持各自来源；日终实际处置决定税款。付款、扣税和阶段回执与账本一起提交。提交前中断回到原状态；提交后确认丢失则读取已保存阶段，不重复入账。

登记日没有持仓也会保存“未登记权益”记录。之后买入或迟到的早期订单不能改变这个记录。普通股份清仓后，应收现金与未结清税款继续构成经济暴露。即使当日付款已结清最后一项权利，该股票当天仍属于实际触及范围；来源缺口不能因为付款先执行而消失。

对于已准入的现金分红规则，盘中按其最高已支持税率预留尚未最终结清的税款，不能用临时零税额释放资金。预留不是现金扣税；实际税款仍由冻结适配器按日终历史计算。没有对应已准入规则的未定税款继续阻止新支出。付款时间、税款时点、取整和账户类别均保留为明确模拟政策，并非实际账户流水认证。

自动公司行动目前支持该适配器的现金分红范围。送转、配股和混合分配会明确拒绝，不能删去股份成分后继续计算现金分红。新接口也不会自行验证供应商公告或补成无事件日。

## 真实八股入口联调

事前冻结范围继续使用 2026-01-06 至 01-08 的原八股与保存的选定盘口。控制器输入为每股 10% 的固定配仓比例，末日显式退出；该比例是工程输入，不是市场因子或模型发现。

| 核对项目 | 保存结果 |
|---|---:|
| 受控程序尝试 | 1 |
| 生成买单 | 8 |
| 来源拒单 | 8 |
| 成交 | 0 |
| 完整现金日 | 3 |
| 初始和期末现金 | 1,000,000.00 元 |
| 费用、股份、已登记权利 | 均为 0 |
| 创建卖单 | 0 |
| 新研究模型调用 | 0 |

费用仍为 `null`，相关义务仍为 pending。公司行动为空仅表示未准入事件，不表示无事件。没有读取原始行情档案、重扫盘口或调用外部数据接口。

独立核对直接读取 SQLite 和保存的 JSON，不再次执行策略、盘口匹配或现金接口。八条买单数量、限价及源盘口行号均与原冻结盘口结果一致；输入哈希、拒单、零盘口消耗和唯一初始本金事件一致。公开分页连续返回了全部 3 个现金日、8 条订单和 8 条拒单。

## 检查及来源版本

35 项相关检查包括：6 项盘口研究入口、7 项动态公司行动、9 项现金接口、5 项原盘口检查，以及 8 项原日线与研究入口回归。另有使用明确保存响应夹具的运行器检查，确认原入口能够接收资金反馈并合法收尾；该夹具不是实际模型调用。

合成资金例子中，400 股实际情景成交分成 170 股和 230 股两个价格档位；每股 0.145 元的示例分红形成 58 元应收，分两次卖出后合计结税 11.60 元，10 万元测试账户期末现金为 100,022.42 元。该金额用于人工算术核对，不是任何股票的真实收益或实际券商费率结果。

八股入口联调使用 34 项检查通过时的版本，源码已封存在该范围的 `source_snapshot`。之后补强了“付款结清当日仍保留经济暴露”的检查，最终 35 项通过；新版本另存于最终验证目录。八股范围无成交、权利或付款，未触及该修复分支，因此没有重跑旧范围。重放旧结果应使用保存版本，不能用当前源码覆盖原证据。

证据：

- [冻结计划](D:/大学/金融投资与量化/ai策略迭代开发/QuantaAgents/experiment_traces/meta_framework_v3/l2_research_bridge_001/plan.json)
- [入口结果](D:/大学/金融投资与量化/ai策略迭代开发/QuantaAgents/experiment_traces/meta_framework_v3/l2_research_bridge_001/result.json)
- [独立核对](D:/大学/金融投资与量化/ai策略迭代开发/QuantaAgents/experiment_traces/meta_framework_v3/l2_research_bridge_001/independent_saved_audit.json)
- [最终检查](D:/大学/金融投资与量化/ai策略迭代开发/QuantaAgents/experiment_traces/meta_framework_v3/validation/l2_research_entry_003/junit.xml)

接下来需要补实际触及股票日、持仓与权利存续期的证券状态、公司行动、费用和可得性资料，再为新研究冻结共同执行条件和资源范围，并实际运行 Astra/xhigh。已超时关闭的 BaoStock 登录不盲目重试，旧 2019 年程序不再按已知结果调参。

两问自检：本次推进了受控程序到盘口资金反馈的可运行链路，真实拒单已穿过研究入口并可追溯。仍未取得合格真实成本样本外收益、新架构比较或冻结前瞻结果；若订单数量受未来盘口影响、已结清权利掩盖当日来源缺口、恢复重复派息或失败丢掉持仓，应保留失败并撤回相应执行结果。本次定向检查未观察到这些违规结果。
