你正在接续用户明确授权的 V9A 因子研究版开发与实际研究任务。请直接执行，完成开发、验证并运行有界真实研究，不停留在计划、设计文档或仅通过工程测试。用户要求主任务使用 gpt-6-astra / ultra，并启用三个 gpt-6-astra / xhigh 子代理。用户的研究目标是产出年度 IC 达到 0.10、跨时间稳定的因子。目标可以未达成，但不能降低口径、伪造结果或把完成软件开发当作已达成金融目标。

工作项目：D:\大学\金融投资与量化\ai策略迭代开发\QuantaAgents。Codex 保存项目的根目录是其父目录 D:\大学\金融投资与量化\ai策略迭代开发，真正仓库在 QuantaAgents 内。主任务已指定 Astra ultra。请在开始工作时实际启动三个子代理，明确设置 model=gpt-6-astra、reasoning_effort=xhigh，按工具规则使用 fork_turns=none 或正整数，给每个代理完整、自包含任务。不要仅在文字中说使用了子代理；核实启动结果。不要为子任务创建额外用户侧任务窗口。

建议三个代理分工如下，主任务负责总体接口、研究协议、整合、运行和结果验收：

1. 因子生成与研究控制：独立 FactorStudy、提案契约、因子族范围、程序调度、错误修复、父代/轨迹、预算和恢复。实现可运行的新结构循环。
2. 数值与因子评价：统一 V6/V7 的股票池/标签/统计口径，共享缓存和批量计算，单因子与固定基线增量，时间切分及有针对性的数值测试。
3. 独立审计与研究设计：核对数据、因果和时间边界，设计匹配基线和反例、检查外部模块移植，独立验证结果及因子交付包；可以实现独立检查与测试，避免与前两者编辑冲突。

主任务先约定代理的模块和文件所有权、接口、验收，随后并行开展。第三代理保持独立复核职责，不以自己实现的同一公式重复计算冒充独立验证。所有代理共享目录，避免相互覆盖。当前仓库有大量已有未提交变更；先记录状态并保护它们，不 reset、不清理原结果、不把旧修改误当本次修改。不要在没有用户要求的情况下创建名义相同的第二套底层引擎。

必须优先阅读并核对当前源码的材料：

- F:\Factor_Framework_Review_20260912\V9A_FACTOR_DESIGN.zh-CN.md
- F:\Factor_Framework_Review_20260912\V9A_FACTOR_DESIGN.evidence.json
- F:\Factor_Framework_Review_20260912\QUANTAAGENTS_REDESIGN_20260912.zh-CN.md
- F:\Factor_Framework_Review_20260912\COMPARISON.zh-CN.md
- D:\大学\金融投资与量化\ai策略迭代开发\QuantaAgents\master.md
- D:\大学\金融投资与量化\ai策略迭代开发\QuantaAgents\docs\research\meta_framework_v9\ACCEPTANCE.md
- D:\大学\金融投资与量化\ai策略迭代开发\QuantaAgents\docs\research\meta_framework_v9\POSTMORTEM.md
- D:\大学\金融投资与量化\ai策略迭代开发\QuantaAgents\docs\research\meta_framework_v9\POSTMORTEM_METHOD_AUDIT.md
- D:\大学\金融投资与量化\ai策略迭代开发\QuantaAgents\docs\research\rd_agent_review_20260912\AUDIT_NOTES.md
- F:\RD_Agent_Reproduction_20260912\REPORT.zh-CN.md
- F:\AlphaAgent_Review_20260912\REVIEW.zh-CN.md
- F:\Factor_Framework_Review_20260912\QuantaAlpha\REVIEW.md
- F:\Factor_Framework_Review_20260912\AlphaPROBE\REVIEW.md

这些设计文档是已认可的实施方向，不是证明现有源码无缺陷。请独立判断具体实现，发现不合理的地方用证据修正。先看本地 AGENTS.md（如果存在）、当前控制器/锁/状态、真实源码和机器验收。旧 V7/V8/V9 实验已经关闭，旧预算不恢复；V9A 使用新的协议、输出目录和账本研究 ID。

目标口径在结果出现前冻结并向用户简洁说明。默认作如下解释，用户后续明确修正时再更新协议并保留历史：

- IC 指原始单因子值（仅使用训练确定的固定方向）与下一开盘起五个交易日原始收益的日度截面 Pearson 相关系数；按每个完整自然年的有效交易日取算术平均。标签语义是 open[t+6]/open[t+1]-1，不是信号后一年收益，不乘 sqrt(252)。同时报告同口径 Spearman RankIC、有效日数、股票覆盖、分组收益、头部相对全池收益和时间依赖下的不确定性。
- 用户目标是年度 IC >= 0.10 且跨时间稳定。完整通过口径应要求预声明多个完整跨期检查年份分别满足目标，而非全期平均达到、只选最好年份或取每年绝对值。首轮可采用 2016—2020 开发、其中按时间折检查，冻结后看 2021—2024 四个完整年度的已曝光诊断；请检查数据完整性并事前固定实际可评年度及规则。
- 因子方向、公式、参数、主要期限、数据选择和必要拟合/更新规则在确认前冻结，不按每年结果翻转方向或重选窗口。参数与公式搜索仅在开发部分；确认结果不回流本轮生成器。
- 主要交付是可执行单因子及用途明确的因子族，单因子、交互、学习型特征、预测器组合分别登记。不能用 LightGBM/Ridge 的组合预测 IC 达到 0.10，替代用户要求的单因子成果。首版优先无需监督拟合的受限公式；学习型特征需独立 fit/transform 时点契约。
- 全期、年度、训练、开发检查、冻结后诊断分别报告。现有 2015—2024 已多轮曝光，即使各年度达标，也只能如实称已曝光历史上的跨期候选；它不自动证明独立样本外稳定、可交易盈利或未来有效。
- 本次没有扩大 2025 数值权限：2025 仅可核对来源、日历和文件元数据，不加载其行情、因子、标签或账户数值。独立稳定性的进一步确认如需要尚未授权数据，保持该项未完成并明确说明，不能为了达成目标偷偷打开。
- 0.10 是研究目标，不是可保证结果。未达到时报告实际最佳值、最差年、失败分母、差距和可证伪的后续方向，不把阈值降低或只报告最佳结果。不要因第一批失败就停止所有已授权开发和预算内研究，也不要无限扩大调用或搜索到过拟合。

已核对的现实基础与待验证项：

- V6 FactorEngine 已支持受限表达式、子表达式缓存、多期限 IC、分组收益、ICIR、分组换手、描述性残差诊断。V7 factor_lab 具有风险、条件、交互和描述性 partial IC。AssetRegistry 已有公式/计算器/面板绑定缓存。先复用和统一口径，不重复造轮子。
- V6 主要使用 panel.eligible，V7 使用 execution_pool；必须确保因子内部截面排名、诊断与目标用途使用声明的一致股票池，不能只在计算后删样本。
- V9.1 ranges.py / batch_compute.py / range_study.py 已写入代码，但现有记录没有导入、烟雾、缓存与原路径数值等价和完整运行验收。不能以“代码存在”跳过验证，也不能把账户数组计算器当成可直接使用的因子批处理器。
- 现有库 516 条目录只有 46 项可执行，并非 516 个可用独立因子。V9 基础收益筛选为 40 项 return 角色；低边际 IC 的风险/条件/交互不能统一淘汰。
- V9 冻结组合的训练 RankIC +0.05149，后期四年为负且前 40 相对毛收益也每年为负；年度开发更新与最终四年固定使用不一致；TMA 是价格水平双重均线且最终拟合才加入重要成分。这些是研究改进证据，不证明某一个修改必然有效。
- RD-Agent 作者重放中，10D+30D 对排序基线的五种子平均预测增量接近零；某个高 IC 代码通过全段拟合生成历史特征，追加未来尾部会改变历史结果。应将时间因果、相同列序/样本/模型设置和配对增量当作验收重点。

推荐实现边界：

- 新增独立因子研究入口，例如 factor_research 包及 V9A 工作流版本，以组合方式调用既有模块，不直接继承被账户/策略绑定的 V9Study。
- 保留数据、受限 DSL、资产、缓存、时间截断、曝光账本、原件、回执和离线恢复。先使用薄适配器复用旧模块，必要重构应有契约验证并保持历史可复现。
- 研究主循环为：冻结协议 → 程序选择机制/父代 → 模型提出少数结构、范围和对照 → 实现检查与有界修复 → 程序批量因子评价 → 固定因子池增量 → 更新研究记录与调度 → 冻结候选 → 确认 → 导出。
- 模型只在机制、结构改变和最终解释等关键节点参与；程序负责大量参数展开、检查、统计与调度。提案必须有预期关系、匹配基线、参数联合约束和具体证伪实验。falsifier 不只是非空文字。
- 借鉴 RD-Agent 的研究/开发职责分离及实现失败经验，AlphaAgent 的假设到公式契约，QuantaAlpha 的修改意图与轨迹，AlphaPROBE 的父代选择和质量/互补探索。直接移植具体代码前核对固定版本许可、依赖与已发现缺陷，不照搬未经验证的 bandit、默认 test 反馈路径或有问题的准入检查。
- 固定批次基线，比较 B 与 B+f；条件比较相同主效应与增加交互。同样本、同拟合设置、同列序和必要配对随机种子；共同样本变化要单独归因。描述性 residual IC 不是组合样本外增量。
- 从模型可见动作和主循环中剥离持股数、调仓、仓位、过滤、缓冲、止损及模型结构的大范围优化。DailyAccount 作为外部固定规则、有小额度的用途检查，保留原引擎，不在因子核心每个参数点运行账户。
- 输出因子包：精确公式/实现及计算器版本、数据字段和时间、股票池、参数/方向/预处理、必要拟合更新规则、主要期限、各阶段证据、基线版本、父代与全部尝试分母。executable、研究证据、交易用途三个状态分开。

必须有辨别力的检查：未来尾部扰动/截断不改变过去因子值（固定数据快照及声明的拟合规则）；严格日期切片与标签端点；字段可用时间和单位检查；空值、常量和 NaN 不冒充成功；修复后实际执行公式重验并更新身份；缓存与原路径、单个与批量数值等价；确认解锁后不能继续本轮训练搜索；同样本的因子池增量；因子包导入后数值重现。测试应针对实际风险，避免只写镜像实现的低价值测试。

执行节奏：先记录源码和当前状态、分配三代理任务；接通一个不依赖账户的现有因子基线；实现新结构和程序批量评价；整合验证；做小型资源校准；然后冻结首轮真实研究协议与有界预算并实际运行。用户已授权本任务的开发和合理的有界研究，不要为常规可逆操作反复索要许可。缺数据或外部权限时继续独立可做的部分，仅就真实缺口询问。

预算按模型调用数、实际 token、候选结构数、参数评价数、确认访问、CPU/墙钟、内存和存储分别登记；包含失败调用与被拒绝候选。不得自动扩大到无界搜索或新增付费服务。沿用可用的本地 Codex 接入与回执；若需要外部收费 API 或购买数据，先说明具体必要性，不读取或输出凭据。

大型缓存与运行结果放到 F: 下新的 V9A 独立目录，并记录绝对路径。开始时实测当前磁盘/内存、每工作进程开销和单批时间，渐进增并行；不根据逻辑核数直接开满。保留原始实验定义、数值证据、失败、数据/代码哈希、回执和实际成本。不要为节省空间删除历史原件。普通 Python 启动可用；此前 Python 3.10 在 -X utf8 模式会因旧 GBK .pth 文件失败，遇到时检查环境，不修改全局依赖来掩盖问题。

完成交付必须包括：实现与相关验证结果、实际启动的三个代理及配置、可运行入口、设计取舍、真实研究协议与完整结果、逐年 IC/RankIC 和基线增量、因子包或拒绝包、实际资源/调用成本、数据曝光与独立确认状态。更新项目文档和主记录时明确 V9A 新状态与旧研究边界，工程完成、研究候选、跨期历史达标、独立稳定性、策略盈利分别陈述。

开始执行后先简洁报告实际分工和第一步，不要再只写一份开发计划。达到阶段结果就给出证据；金融目标未达到时保持诚实，同时完成所有已经授权且可推进的工作。
