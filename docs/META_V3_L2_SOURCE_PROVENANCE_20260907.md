# V3 L2 转换来源与原始记录核对

2026-09-07。找到了本地转换程序、批处理日志和两股原始小样本。**2026-02-10 两股的 9,774 条行情记录、69 个字段，共 674,406 个单元格，经独立解析后与现有 Parquet 全部一致。** 这补上了这两个股票日的实际字段保留证据。没有运行原转换程序、改动数据源或计算策略收益。

## 能确认的转换行为

来源是 `F:\2026(QQ群1097616051后续增量更新)\l2_parquet_pipeline`，与此前保存的行情库位于同一父目录。本轮保存了程序、测试、日志和此前四份清单，共 12 个文件、3,427,175 字节。文件逐个记录内容哈希、大小和修改时间，读取期间原文件身份未变。

当前程序 `convert_l2_to_parquet.py` 的 SHA256 为 `9bc3fbd4952cd00a44e3f2487780dffd241726225f13818856a17670e81d5bad`。逐段检查得到以下事实：

| 字段 | 当前程序处理 | 证据范围与限制 |
|---|---|---|
| 十档价格与数量 | 原列清理 NUL、首尾空白后转整数；没有乘除 100 或 10,000 | 列名 `_x1e4` 及查询视图除以 10,000 表达本地价格口径，仍不能替代供应商单位说明 |
| `event_ts` | 自然日与补齐到 9 位的源时钟拼接，按毫秒解析为无时区时间 | 没有接收时间，也没有测量网络延迟；香港时间解释仍是明确约定 |
| `source_row_no` | 由读取行序计算，每个源代码从 1 起编号 | 不是交易所跨表序列号，不证明同时间戳事件的经济先后 |
| `bs_flag` | 原始 `BS标志` 列清理后保留 | 此转换程序没有推导主动买卖侧；上游如何产生该字段仍未知 |
| 成交量、成交额修正值 | 特定两列对超过有符号上界的 uint64 文本减去 2 的 64 次方 | 原始负修正量被保留；不能将它们静默截成零 |
| 空值与结构 | 固定表头检查；空字符串转空值；必要输出列另做非空检查 | 流程中的检查代码存在不等于所有旧批次均由相同版本运行 |

原始表头中的“万得代码”只是列名，不能据此把数据供应商认定为 Wind。当前程序没有账户费率、证券状态、公司行动或行情接收字段。

## 两股原始记录与现存文件的对照

样本范围直接沿用旧转换测试已经写定的 `20260210 / 000001.SZ / 600000.SH`，没有按本轮结果选股。单独冻结计划后，读取两份原始行情 CSV 共 4,217,757 字节，并核对对应现存 Parquet 的完整文件哈希。没有读取原始逐笔成交、逐笔委托，也没有用本轮程序重新转换和覆盖库文件。

独立核对程序用标准 CSV 解析器重建字段、日期、毫秒时钟和原行号，再与现存 Parquet 的全部 69 列逐单元格比较；没有导入或运行原转换器。

| 样本 | 原始行数 | 核对列数 | 单元格数 | 差异 |
|---|---:|---:|---:|---:|
| 000001.SZ / 2026-02-10 | 4,797 | 69 | 330,993 | 0 |
| 600000.SH / 2026-02-10 | 4,977 | 69 | 343,413 | 0 |
| 合计 | 9,774 | 69 | 674,406 | 0 |

两个现存物理文件完整哈希读取共 86,865,007 字节；代码列压缩载荷 2,483 字节；命中行组的完整字段压缩载荷 14,704,249 字节。以上是原内容哈希读取与 Parquet 列块载荷，未将其冒充操作系统实际磁盘 I/O。原 CSV 和核对用小缓存均保存，后续可从小缓存复查。

这证明的是两个股票日的字段保留及确定性规范化。它不能外推到上一轮一月八股、所有 132 个交易日或上游原始采集的完整性；也没有认证数量单位的经济含义、真实成交能力或收益。

## 仍未补齐的历史来源

一月四份清单和日志记载，其文件在 2026-07-23 生成，并记录原压缩包被删除。当前程序修改时间为 2026-07-25T01:58:36Z；四份清单均没有转换程序版本、代码哈希或接收时间字段。**当前代码与批处理日志只能提供可检查的线索，不能补写为一月文件生成时的精确程序版本或一月实时到达记录。** 本轮没有删除任何源数据。

本地项目的费用配置检索未找到实际券商合同；已有费用研究材料也明确区分公开收费规定和特定账户佣金。现有配置数值不能自动升级为实际账户费用。未对其他项目或整个磁盘的个人文件作穷尽搜索。

当前可用于研究的范围仍是来源和盘口参考证据。历史接收记录或可证延迟界、供应商单位定义、固定股票日的证券状态与公司行动、适用规则及实际账户费用，均需分别补证；未知条件不能按零处理。正式执行资格、PnL、Sharpe 和总体目标均未通过。

## 状态与公司行动来源尝试

已有 BaoStock 官方文档和此前成功返回的单事件记录提供了一条明确候选路线。本轮另行冻结原八股的 2026-01-06 至 01-08 不复权日线状态查询，以及 2026 除权除息年份的分红查询，最多 18 个请求，单连接、10 秒接收超时，不重试、不翻页。

实际只发出 1 次公共匿名登录请求，收到 0 字节，SDK 返回 `10002007`（网络接收错误），运行输出记载接收超时。所有八股行情状态与分红查询均未发出；连接已关闭，该范围按规则结束。不能把这次网络失败解释为股票无分红、接口永久不可用或候选来源已完整验收。详情见[来源尝试回执](../experiment_traces/meta_framework_v3/l2_state_actions_001/summary.json)。

## 证据与后续工作

- [本地来源冻结计划](../experiment_traces/meta_framework_v3/l2_provenance_evidence_001/plan.json)
- [原始记录对照冻结计划](../experiment_traces/meta_framework_v3/l2_raw_roundtrip_001/plan.json)
- [全部逐股核对结果](../experiment_traces/meta_framework_v3/l2_raw_roundtrip_001/result.json)
- [上一轮八股盘口报告](META_V3_L2_EXECUTION_EVIDENCE_20260907.md)

两问自检：本轮补到了实际转换代码和原始记录一致性，消除了两个股票日“仅按列名猜测转换”的缺口；一月生成版本、采集时钟及账户条件仍缺证。状态与公司行动补证因登录接收失败未取得数据。下一步应使用新证据支持的来源路线或已恢复的外部连接，补固定股票日的状态、公司行动和适用费用来源，并确认是否存在供应商与账户原件。若原件与当前字段解释冲突，应保留本轮结果并拒绝相应字段进入资金执行；不能修改旧结果来掩盖冲突。本轮所有来源读取范围结束后关闭，不为追求一致性重新挑样本或重跑。新增研究模型调用和策略收益回测均为 0。
