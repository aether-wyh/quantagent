# 私有仓库范围与迁移说明

导出时间（UTC）：20260917T033303Z。目标仓库：`xiezhongyan2015-debug/quantagent`。

## 包含的内容

- 原项目的源码、脚本、测试、实验 YAML、因子提案、设计文档、迭代记录和小型审计证据。
- 原 README 保存在根目录 `README_LEGACY.md`，以保留其相对链接和历史内容。
- `legacy_examples/` 保存早期两套策略、曾选存的实验和一组被否决实验的源码、配置与小型来源记录；目录命名不代表盈利认可。
- `docs/archive/` 保存早期交付说明、精选报告与汇总统计。部分报告引用的完整明细仍只在本地。

## 排除的内容

真实 `.env`、旧 `.git` 历史、虚拟环境、缓存、浏览器会话、运行日志、备份 ZIP、行情数据、大型面板、逐日逐股信号、持仓和交易明细没有进入此次提交。`research/factor_research/v9a_builtin_proposals.json` 是研究定义，予以保留。

外层交付 ZIP 和 RD-Agent 源码 ZIP 未纳入仓库。RD-Agent 的审计清单仍指向当时的来源快照，原 ZIP 留在本地；其原始许可证另存为 `docs/third_party/rdagent_LICENSE.txt`。

## 导出时的变更

只在上传副本中替换了首页、补充了忽略规则、A 层依赖清单和本说明；10 处数据库连接代码及旧 README 中移除了内嵌密码默认值，继续使用 `DOLPHINDB_PASSWORD` 环境变量；对应测试改为检查无默认密码的行为，并隔离环境变量。没有修改原工作目录，没有带入旧的敏感提交历史，也没有重写历史研究结果。

`requirements-alayer.txt` 根据源码的直接导入整理，尚未在新环境进行完整安装验收。部分扩展脚本还可能需要额外依赖，例如序列模型相关脚本。

## 仓库外的运行依赖

当前代码仍引用原机器路径，包括日线与分钟行情、`F:/A_Layer_Research`、`F:/A_Layer_OOS`、`F:/A_Layer_Live`、旧 V10A 因子目录，以及本机 Alpha101/191 和因子日历模块。

迁移时检查 `src/quanta_agents/factor_lab_a/external.py`、`competition.py`、`minute_features.py` 和命令行参数中的默认路径。仓库不附带这些外部库与数据；先根据来源记录准备依赖，再配置数据位置。文档中的历史绝对路径保留用于本地证据追溯，在 GitHub 上不一定可打开。

## 来源和访问边界

保留已有第三方文件的版权与许可标记。该私有快照未新授予整个项目的开源许可证；若以后公开，需另外整理完整的来源与许可清单。真实凭证需要在运行环境独立管理。

本次验证限定于文件筛选、敏感内容检查、Python 静态语法检查、数据库无默认密码的定向测试、提交内容和远端私有状态；未启动研究任务、重算收益或运行完整框架测试。
