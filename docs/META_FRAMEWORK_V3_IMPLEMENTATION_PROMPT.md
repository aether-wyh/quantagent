# QuantaAgents 元框架 V3：实际实施、研究与策略交付

项目：`D:\大学\金融投资与量化\ai策略迭代开发\QuantaAgents`。

用户于2026-09-07在完成V2复盘后明确要求：“修改元框架开启V3版本，并在新窗口中继续完善框架生产策略，目标夏普>1。”这是新的V3实施与研究授权，取代此前仅允许收尾的限制；不是延长旧实验或宣告已有策略达标。所有正式研究、摘要、专家、元评审和架构比较继续使用 **gpt-6-astra / xhigh**，不得以小模型代理行为评测。

请直接实施、测试、运行有界研究并按结果改进，不只提交计划。任务在本项目本地继续，保留已有未提交改动。V3的用户目标仍是：普通输入与正常文档→自主查证、开发、诊断和迭代→扣除真实交易成本后冻结样本外净Sharpe严格大于1，并按跨案例、独立重复和前瞻验证标准判断稳定性。市场没有足够优势时可以真实弃权，不能保证任意想法盈利。

## 开始先读的最小材料

1. `docs/META_V2_WAVE_RETROSPECTIVE_20260907.md`，理解真实进展、研究未闭环与投入产出问题。
2. `docs/research/meta_framework_v8_handoff/checkpoint_20260907_031.json`：这是V2最终关闭快照。`docs/META_CONTROLLER_CHECKPOINT.json`仍是早期P0历史，不能据此恢复。
3. `docs/research/meta_framework_v3_handoff/launch_manifest_001.json` 与 `control_transfer_001.json`，核新窗口主控身份、V3源码与已有监控接管状态。
4. **完整阅读** `docs/META_FRAMEWORK_IMPLEMENTATION_PROMPT.md`；其中P0–P5与成功标准继续适用。本文件调整实施优先级和新版本启动授权，不放宽收益、执行或统计门。
5. 按需读取 `docs/META_ASHARE_PROTOCOL_DRAFT.md`、`docs/META_ASHARE_BENCHMARK_CASES.md`、`docs/research/meta_framework_v18_handoff/research_loop_budget_audit_001.md`，以及下面绑定的工具与保存验证报告。不要重新通读所有修订或重跑所有测试。

## 唯一控制权与旧账

当前交接父任务为 `01a0774d-4327-7701-948f-2796de6fccd3`。新窗口是唯一V3实施与研究主控；父窗口只完成交接，不同时派发。以本任务实际threadId核对移交文件，先检查研究worker、进程身份和租约。创建新任务后的几秒内移交文件可能尚未出现或仍是prepared：可以先只读检查，等同一移交记录补齐真实targetThreadId，不能猜ID或重复创建窗口。

已有小时监控只有 **a**。父任务会将它迁到新任务并恢复为ACTIVE；先查看实际配置，不新建第二监控。主控正在工作时心跳只读；空闲时按最新V3检查点继续有明确下一步的工作。状态不变安静，仅实质进展、故障或需用户介入时汇报。8776是已有只读历史研究页面，页面可用不等于研究进程运行。必要的升级使用明确版本和进程身份；旧8769/8773有过停止审批拒绝，不绕过。

V2原四题仍paused：15传输、14完成，491954已知token与80000原未知预留；首题14查询/12候选/0final，后三题未开始。原六小时expired_incomplete永久保留，v18s1唯一补充窗截止2026-09-07T10:16:47.524738Z，不因新V3授权顺延、不自动第三窗。V3从新的、事前冻结的范围开始，不重开旧四题预算或给首题补调用。若以后确需用旧资料，只能显式标为派生开发，保留全部旧费、失败、历史暴露，不算新独立样本。项目总账继续列出原未知80k，不把新账域当消债手段。

旧Q三份回答的P0恢复已完成，涉及历史61758token；不重问、不再导入当新增研究。已验收的外部预算管理器未实际应用，V3启动不需要为旧首题做行政跳题。

V3命名使用 `src/quanta_agents/meta_v3`、`experiment_traces/meta_framework_v3`、独立registry kind；不得复用旧 `meta_ashare_revision3` 或既存 *_v3 历史目录。不要把项目包总版本改成已完成的3.0正式版。

## V3已经落地的起点及其局限

父任务新增 `src/quanta_agents/meta_v3/closing.py`：可配置的任务/阶段名义额度、调用槽和收尾时间；输出explore/close_only/blocked/terminal_without_submission，并据此生成公开允许动作菜单。新V3不携带旧题未知调用的特殊豁免。`__init__.py`明确标记3.0.0-dev、RUNTIME_INTEGRATED=False。

这是纯计算与菜单过滤，还没有原子预算事务、持久状态、实际schema/dispatch强制门或研究调用。通过其7个定向测试不能称V3研究入口已完成，也不能称供应商账单绝对受控。原v19纯策略与10项重放、v18预算管理器作者5加补1/独立8的证据可读复用，不机械重跑。

## 立即实施的顺序

**一，接通可结束的研究运行。** 在新V3同域事务内持久化explore/close_only模式、调用意图与预算预留，既检查每题也检查共享阶段；至少保护一次名义收尾额度、一个调用槽和一个final机会。到收尾阶段，公开schema和实际动作准入都只能提交最终研究报告（含有证据的合法弃权），恢复不得再开放探索。模型拒绝交稿、schema不合法、超时/未知、截止等保持真实失败；无预算则明确无提交终态，不由主控补写final。未知/pending优先阻断，不能自动释放费用或重试付费。

复用现有完成回执、幂等应用和来源核验能力。必要验证覆盖预算竞争、提交前后崩溃、模式恢复与非final动作拒绝，使用有限夹具/保存事件；先修具体反例，不为每个小改动复制整个代码树或无关大测试。

**二，接通一个公开的普通输入入口。** 原 `scripts/run_research_loop_v2.py` 仍进入旧workflow_v2，不能把并列meta组件当成已替换。在V3建立清晰可运行入口：加载一句话/文档→研究器主动查证→受控因子/策略程序→可信执行反馈→最终报告与自检。工具至少支持已有期限、条件、匹配、实际持仓成本诊断和受控开发；根据真实需要接线，避免为了齐全先重做所有工具。

不要保留必须每轮列固定假设数、只能沿单一accepted strategy、永远不能主动诊断的机械限制。允许有限竞争分支、理论修订、停止无信息搜索；所有候选、内部扫描、失败与成本登记。研究器不能修改内核、评分、留出权限或预算。事实/工作假设/反证/未知分开，自己运行的记忆有来源与失效条件，跨任务隔离。

**三，优先取得一个真实研究终态。** 先用离线小例确认完整接线，再在全新、事前登记的有限开发任务上真实运行Astra/xhigh，并按冻结量表评审唯一终稿或合法弃权。使用清晰正负控检查工具可达与错误宣称，再推进真实开放题；不要重复原两道诊断题直到成功，或把旧四题改名当未见题。校准/已暴露材料只能算开发，不能计正式策略成功率。

首个闭环的必要证据来自**同一入口的真实研究轨迹**：普通输入、实际工具使用、冻结受控程序或明确可检验结果、对应资金/失败证据、模型最终报告。纯函数、固定demo、fakeGateway或人工写出的策略都不能替代该终点。若当前真实执行资料不齐，可先做明确标注的合成联调或declared simulation来找工程错误，但不能将它们输出为合格净收益策略。

**四，补齐真实执行反馈再扩策略研究。** 846股×1217会话的数据盘点已关闭，不重扫原CSV。两股16会话的192项机械义务仍pending，不因已有盘点变为通过。复用保存清单，针对当时可用数据、股票池、停牌涨跌停、公司行动、费用和保守容量补有界证据，优先把事前固定的小范围认证完成；范围选择不看盈亏。不删除持有中不利事件或缺价，不改变资金分母，未知权益不得静默为零。

原价、实际股份、完整现金日、整数手、T+1、部分成交、拒单、现金/股份权益与真实成本进入同一可信执行器。事件毛收益与组合净收益分别报告，costs_applied=false不能成为净Sharpe反馈。受控策略桥相关实现/边界见 `docs/research/meta_framework_v17_handoff/strategy_development_001.md`，不能误以为已在真实策略研究运行。

**五，再做架构和正式策略验证。** 共同工具内核固定后，比较强单agent、简单固定搜索和候选harness。主轨道固定模型、资料、工具与全栈资源规则，允许自主动作分配；窄消融再固定机会以识别具体作用。冻结改动后在未见机制、输入表述和文档上独立启动，保留失败、正确弃权与成本，不能用旧三组开发正差认定架构优胜。

2017–2021是开发；2022–2023已暴露；2024–2025仅在当前流程封存，不声称全项目或预训练从未接触。正式样本外至少252真实交易日、30退出批次、回撤≤25%，完整资金逐日净收益以252和样本标准差计Sharpe，年化无风险2%并报0敏感性，净Sharpe**严格>1**且全部执行/覆盖/容量门合格。至少6个真实留出案例、3机制族、每题每架构3次独立研究启动；成功率≥2/3、有效策略Sharpe中位数>1、预注册单侧成功率下限>1/2，方法尊重题族/重复/共同市场日期相关性。最低样本不自动保证功效。架构配对增益和策略达标分别检验；随后必须有冻结后的新增数据/纸面执行，事前冻结覆盖、收益风险、执行偏差及通过/失败/继续观察门。

## 成本、迭代与持续工作

用户允许必要的大量token投入；重点防重复调用、失控循环与并发预算失效。先冻结每个新阶段的有界预算、停止条件和范围，技术准入由已授权主控完成，不为日常合理实验重复向用户索取许可。父任务closing默认数值是开发起点，不自动代表所有未来正式实验预算；依据校准成本在看新结果前冻结。不要因低分/高分临时加轮或改规则。新unknown先对账；已保存回答离线恢复，结果未知不自动重复付费。

研究Gateway、规划/评审/摘要/专家/元设计的用量和耗时都应记录；未知不当零，cached/reasoning子项不重复相加。平台goal计数不是供应商账单，不与研究账本简单相加。原V2多次输入成本占96–98%，应把完整原件保留与每轮工作状态分开设计；证据索引、按需取证和摘要须能回到原件，保留失败/反证。先作为独立改动验证质量，再用于公平比较，不偷偷删历史。

每次改进记录两问，并在GUI显示公开摘要：做得怎么样，证据/缺陷/不确定性是什么；下一步是什么，什么结果会推翻判断。不要把篇幅、角色数、版本数或测试数当奖励。验收通过就继续有信息价值的下一步，不陷入无限审计。GUI同时显示V3实际活动、公开研究摘要、工具与策略、预算/用量/未知、暂停原因与最终状态；不得显示为隐藏思维链或把保存状态冒充进程活性。

在本新任务创建或沿用上述明确目标，不设未经用户要求的token总预算。持续实际推进直至有充分证据达标或真正外部阻塞；等待新增数据期间保持正确未达标和有意义监控。禁止实盘下单、资金操作、购买服务、未授权外部发送或修改账号/凭据。需要用户介入时说清具体缺口；其他正常研发直接完成。

现在先核移交、读上述材料，完成V3原子关闭门与普通输入入口的最小接线，然后实际测试、研究和评审。旧范围不重启，新版本不靠改标签制造成功。
