# 提案费用展示与运行时身份接口核对

既有8777研究页面已纳入关闭的生成数据提案账本。页面从53次调用、1,365,482已知token更新为57次调用、1,427,801已知token，待核对研究预留仍为0。新增的是原来已发生的4次调用和62,319 token，本轮没有新增模型调用或费用。

新增任务按计划来源识别并显示为“模型参数提案 · 生成数据”。费用说明明确区分请求模型设置与运行时身份确认，显示4次调用各自用量、模型弃权终稿、三槽与144扫描单元，以及实际账户执行为0。共同模板的Nonzero fees失效声明和正式成功分母0也保留在页面。旧8项任务及3份证据说明逐项保持不变。

服务更新前核对了8777监听进程及其启动器的PID、创建时间、命令和工作目录。仅重启这一组服务，保留所有已有账本和证据参数；新服务隐藏运行。8769和8773未操作。HTTP检查通过，实际Chromium浏览器确认汇总指标、新任务标题及身份说明；汇总与提案区截图已视觉检查，位于`output/playwright/proposal_cost_023`。浏览器的唯一资源错误为favicon404，未观察到页面脚本错误；核验所用独立浏览器已关闭。

Computer Use入口因本机资源路径缺失未启动，改用离线缓存的Playwright CLI。初次页面快照先于数据返回，后续完整快照已加载；一次局部快照引用过期未取得任务单独快照，任务标题已由完整快照和HTTP确认。相关工具故障均记录在验证摘要，不据此重新运行研究。

[官方非交互模式文档](https://learn.chatgpt.com/docs/non-interactive-mode)说明JSONL线程、轮次和条目事件及结束用量；该节的示例未建立模型及推理强度的运行时身份凭证。使用4次原调用保存的可执行文件，仅运行`exec --help`，确认本机存在请求模型和JSONL输出选项，但该帮助未找到身份凭证专用输出开关。这不能推导为所有受支持接口都不存在此能力。

结论仍是：当前四次调用的运行时身份未确认。没有改解析器，没有从请求设置回填确认，没有推断具体回退模型，也没有读取或改变凭据。未来若研究替代接口，应单独冻结其能力与验证范围，不能借此重跑已关闭提案。

本轮费用页面和只读文档核对的证据在`validation/proposal_cost_view_001`。第022阶段的26份修复交接材料全部保持原哈希；父任务仍独立复核021-C01/C02。全目标仍未达标。
